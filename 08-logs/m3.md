# M3 — implementation log (Graph core, THE PIVOT)

Per-task build narratives for **M3** — what was built, the independent review, and the
verification. The plan, build decisions, acceptance criteria, and the live task checklist live in
[../08-implementation-plan.md](../08-implementation-plan.md) (§M3). This file is append-only detail;
nothing here is a contract — the contract is 00–09 + the ADRs.

## Task 1

**Migration 005 + config/vocab plumbing** — server commit `f2b9549`, local, **not pushed**.

Migration 005 (`005_graph_pivot.py`, [ADR-030](../adr/030-entity-substrate-and-lifecycle.md)/
[031](../adr/031-m3-organizer-and-contract-extensions.md)/[032](../adr/032-prior-art-adoptions.md),
[02 §3](../02-data-model.md) v3.2): drops `note_links`/`chunks`/`notes` (fresh start — nothing
copied); creates **`nodes`** (id = frontmatter uuid, **no column default** — the indexer always
supplies it; `aliases` **GIN** = the alias index, `disambig`, `occurred_start/end`,
`organizer_version`, `merged_into` as a **plain uuid, no self-FK** so reindex insertion order never
matters; HNSW cosine at 768), **`chunks`** re-keyed `node_id`, **`edges`** (pk
`(src_id,dst_id,rel,origin)`, one `score` column for both origins, `since`/`until`, cascade FKs +
`edges_dst_idx` — the reverse index merge needs), kind-generic **`review_queue`**; renames
`captures.note_paths → node_paths` (rename-only; old rows keep pre-pivot paths as historical
locators). `summaries` untouched (kept until M10). **Verified up→down→up against a real pgvector
DB** (dev compose); `\d` output matched the 02 §3 contract exactly.

Config ([ADR-031](../adr/031-m3-organizer-and-contract-extensions.md) Consequences): new settings
`graph_store_path` (dev `../graph-store`, prod `/srv/graph-store`), `graph_store_repo` (empty ⇒
bootstrap skips the remote — dev semantics), `node_types` (the 9 seeds), `edge_rels` (the 6 seeds),
`entity_like_types` (the ADR-030 six — made a setting per CLAUDE.md rule 9 "no hardcoded lists"),
`entity_match_min_conf` (0.8, `ge=0/le=1` bounded), `mcp_capture_max_inflight` (**2** — implementer
default, live-tunable), `profile_refresh_cron` (**04:10** — the retired daily-summary slot, after
the 03:40 reindex so the day's edges are in the DB). Boot-time vocabulary guards: `memory` must be
in `NODE_TYPES` (organizer fallback type); `ENTITY_LIKE_TYPES ⊆ NODE_TYPES`. `.env.example`
documents all keys. **`vault_*` settings deliberately coexist** with the new `graph_store_*` keys
until task 2 performs the code rename (CLAUDE.md pivot note); nothing reads the new keys yet —
plumbing lands first by design. Day/night effort defaults (ADR-032 §4) are ADR-025 *routing
configuration* (app_settings), not env keys — they land with the organizer/routing work, not here.

Tests: config-head test advanced to 005; new tests for the seeded vocabulary defaults, CSV
coercion (types/rels/entity-likes) and both guard rejections. Full suite **260 → 263 passed**,
`ruff check` green.

**Independent review** (fresh agent, diff vs 08 §M3 + ADR-030/031/032 + 02 §3 + CLAUDE.md):
**no must-fix findings** — every fixed column/setting verified present and correctly typed; scope
clean (nothing from tasks 2–10 leaked in). 5 minors: 4 fixed inline (stale scheduler-slot comment;
`entity_match_min_conf` range bound; downgrade-loses-review_queue docstring caveat; `edge_rels`
CSV-coercion test gap); 1 logged as follow-up ↓.

**Follow-ups:**
- Task 7 (vocabulary surface): document `ENTITY_LIKE_TYPES` in 02 §3's config list — it extends
  the ADR-031 config set, and a governed addition of a new *entity-like* type needs the env list
  updated too (reviewer minor #2).

## Tasks 2 + 3 + 5 (combined — the graph rewrite)

**Combined by the user's call at kickoff.** Tasks 2 (store writer + rename + bootstrap), 3
(organizer v3 + entity resolution), 5 (indexer/search retarget) are one atomic ~1300-reference
rename entangled through the capture pipeline, and migration 005 had already broken the indexer
against the live schema — so slicing them apart would leave the suite red at a task boundary. Done
as one rewrite with a single independent review. Server commits `2798412` (+ review follow-ups
`c54fed2`), local, **not pushed**. 240 unit tests pass, `ruff check` green.

**Task 2 — store writer + backup + bootstrap.** New `graph/node_writer.py` (`NodeWriter`): writes
`<type>/<slug>--<shortid>.md` (memory prefixes the date), the full frontmatter contract
(id/type/created/occurred?/source/source_ref?/plane?/planes/tags/aliases?/disambig?/
organizer_version/edges), atomic + collision-safe; each node gets a **fresh uuid** (`source_ref`
carries the capture id — siblings share it, ids are distinct so the DB pk + edges stay unique).
`services/vault_backup.py → store_backup.py` (`VaultBackupService → StoreBackupService`): ADR-014
machinery behaviour-identical, bootstrap skeleton = the 9 node-type folders + `inbox/`, `.gitignore`
drops `.obsidian` (Obsidian gone), remote wired from `GRAPH_STORE_REPO` via a new
`GitClient.set_remote` (zero-touch cutover, ADR-031 §6). Wholesale **vault → graph-store rename**
across config/`.env.example`, `git_repo`, `backup_jobs` (agent `vault-backup → store-backup`,
R2 `vault/ → store/`), `scheduler`, `system_health` (`/health` leg `vault → store`), `cli`,
`reindex`, `capture_store` (`note_paths → node_paths`), `models`, all routers, `dependencies`,
`main`. Deleted `capture/notes.py` + `graph/renderer.py`.

**Task 3 — organizer v3 + entity resolution.** `capture/organizer.py` rewritten: prompt emits
typed nodes vs the 9-type vocab (no fit → `memory` + a `node_type` **vocab-proposal**), `occurred`
kept only as valid partial-ISO (never fabricated), entity **mentions** (`{name,type,rel,disambig}`)
validated against `ENTITY_LIKE_TYPES`/`EDGE_RELS` (unknown → dropped + proposal), injection hygiene
(capture behind hard delimiters, declared data). New `entities/` package: `store.py` (`PgAliasStore`
— exact-on-normalized candidate lookup over the `nodes.aliases` GIN, tombstones excluded) and
`resolver.py` (`EntityResolver`): per-mention **exact short-circuit** (1 candidate → auto-link, no
LLM), **mint-new** (0 candidates → thin-hub `NodeDocument`), **multi-candidate → LLM disambiguation**
with structured candidates gated by `ENTITY_MATCH_MIN_CONF` (choice/conf; below floor or chain-down
→ **`entity-ambiguity` review item + edge left pending, never guessed**), **intra-capture dedup**,
**entropy guard**. New `services/review_queue.py` (`PgReviewQueue.enqueue`) — the kind-generic queue
**write path** (task-4's read/resolve UI still open). Pipeline rewired: organize → resolve mentions →
build `NodeDocument`s (content nodes get fresh ids + resolved entity edges with `since = occurred ??
created`; minted entities appended) → write → index; vocab proposals filed; all resolution best-effort
(a resolver failure leaves nodes unlinked, never fails the capture — rule 2).

**Task 5 — indexer/search retarget.** `indexing/frontmatter.py`: parses the node contract incl. the
`edges:` block list + partial-ISO `occurred` → date range + `merged_into`; id falls back to a
path-stable uuid5. `indexing/store.py` (`NodeUpsert`, id-keyed upsert, `update_node_path` for moves,
`replace_canonical_edges` materializing only edges whose target exists). `indexing/indexer.py`:
id-keyed, **whole-file** content hash, two-phase (upsert all, then materialize edges so same-batch
targets satisfy the FK). `graph/store.py`+`service.py`: derived `similar` edges **DB-only** into
`edges(origin='derived')` (renderer deleted, no file writes). `search/store.py`+`service.py`:
`SearchHit`/`NodeRow` typed, `types` filter, `GET /nodes/{id}` returns edges (canonical+derived,
both directions) + `profile` (null until task 6) + tombstone; router **302-redirects** a tombstone
to its survivor. `chunk_note → chunk_node`; `sb:related`/co-capture stripping deleted.

**Verification.** Beyond the 240 unit tests: a **Pg-store smoke test against a real migration-005
pgvector DB** inserted node/chunk/edge/alias/review rows and exercised every plain-SQL path
(`PgIndexStore`/`PgSearchStore`/`PgGraphStore`/`PgAliasStore`/`PgReviewQueue`) — it **caught a real
bug**: `replace_canonical_edges` compared a str `dst_id` against asyncpg's `UUID` objects, so
canonical edges silently never materialized; fixed (`str(r["id"])`). The full capture→…→search flow
was **not** run end-to-end (no live claude-max/Ollama) — that is the M3 Accept (task 10).

**Independent review** (fresh agent, diff vs 08 §M3 + ADR-030/031/032 + 02/03/04 + CLAUDE.md):
**no must-fix findings** — the core flow (resolve → mint/link/review → write → index → materialize
→ search), invariants (never-lose, never-guess), and the contract all hold. 5 minors: 3 fixed inline
(`chunk_node` rename per the pivot rule; stale `graph`/`indexing` package docstrings; docstrings that
overclaimed cross-capture alias collapse); 2 logged ↓.

**Follow-ups (from the combined review):**
- **Alias accretion + fuzzy matching** (reviewer #1, the substantive one): resolution is
  exact-on-normalized only, and a successful link does **not** append the new surface form to the
  matched entity's `aliases`, nor does the organizer elicit per-mention `aliases`. So "Alex" then
  "Alexandru" across captures → **two** `person` nodes. Repeated *identical* mentions collapse
  correctly. The literal M3 Accept ("mentioned twice → one node") holds for identical forms; the
  Alex/Alexandru case needs alias accretion (rewrite the entity's frontmatter aliases on link) +
  the organizer emitting aliases, and/or `pg_trgm` fuzzy matching (ADR-032 defers fuzzy). **Wire at
  task 6 (entity services) or before the task-10 Accept.**
- Surface entity-resolution outcomes (resolver `model_used`/`fallback_used`, pending count) into the
  capture's `agent_runs` details for observability (reviewer #3, rule 3/7). Review items are already
  persisted, so nothing is lost.
- Incremental `reindex_all` re-materializes canonical edges only for re-indexed (non-hash-skipped)
  nodes; near-unreachable today (thin entity targets can't fail to index), a DB-wipe reindex restores
  all — but tighten `replace_canonical_edges`'s "reconciled on next reindex" promise if the thin-target
  guarantee ever weakens (reviewer #2).
- Dead `EntityResolver` `since` param + unused `entity_alias_min_fuzzy_len`/`Mention.aliases` — remove/
  wire when fuzzy + accretion land (reviewer #5).

## Task 4

**Review-queue read/resolve surface** — server commit `c53eadc`, local, **not pushed**. 265 unit
tests pass (25 new), `ruff check` green, real-DB smoke on the new SQL passed.

The write path landed in task 3 (`PgReviewQueue.enqueue`); this adds the admin **read/resolve**
half over the same kind-generic `review_queue` table (03-api §Review, ADR-030 §3 / ADR-029).

**Store (`services/review_queue.py`).** Added `ReviewRecord` + a `ReviewReadStore` protocol
(`list_items`/`get`/`resolve`) alongside the existing write-only `ReviewQueue`; `PgReviewQueue`
implements both (plain SQL). `resolve` is a **guarded** `UPDATE … WHERE id=$1 AND status='pending'
RETURNING id` so a double-resolve is a no-op. Status constants added (`pending/resolved/discarded/
maybe`). jsonb payload/resolution decode tolerantly (text-or-dict, like `agent_runs`).

**Service (`services/review_service.py`, new).** `ReviewService` holds the resolution business
logic the router delegates to (rule 5). `list_items` (status defaults `pending`; `all`/empty drops
the filter). `resolve` dispatches by kind:
- **entity-ambiguity** — `choice = <candidate id>` materializes the pending edge; `"new"` mints a
  thin entity hub (title + alias, source `review`) then materializes onto it; `"maybe"` defers
  (status `maybe`, draws nothing). Materialization reuses the store's own machinery: `NodeWriter.
  add_edges` appends the edge to each source node's file (atomic, idempotent), then the **indexer
  re-reads that file** and materializes the canonical edge into `edges` (never a direct DB write —
  rule 1); a store commit is requested (ADR-014). For `"new"`, the minted entity + source files are
  indexed in **one** `index_paths` call so the two-phase indexer upserts the entity before the
  source edge's `dst_id` FK is checked. A source node that isn't indexed / has vanished is skipped,
  never fatal (rule 7).
- **vocab-proposal** — `verdict = "reject"` discards; `"approve"` records the verdict + opens a
  **queued `vocab-consolidation` marker run** (SKIPPED, `details.queued=true`, carrying `vocab`/
  `value`/`review_id`) for visibility (vision P8). **Task 4 only queues** — the retro-consolidation
  job that mutates the live vocabulary + re-walks the graph is **task 7**, which consumes these
  markers / the resolved vocab-proposal items (**user call at kickoff: verdict + queue-hook now,
  mutation in 7**).

**Write-path enrichment (the piece task 4 depended on).** An `entity-ambiguity` item filed by the
resolver did **not** carry the source node id the pending edge originates from (the resolver files
the item before content-node ids exist, and pending mentions are skipped, so nothing on disk records
them). Fix: the pipeline now **pre-assigns each content node's id** and builds a
`pending_edges_by_key` map (`{mention_key: [{src, rel, since}]}`) passed into `resolver.resolve`;
`_file_review` stores it as `payload.pending_edges`, and `_build_documents` reuses the same ids so
file ↔ review agree. Purely additive to the resolver/pipeline (optional kwarg; existing tests
unchanged). `since` matches what `_build_documents` stamps (`occurred ?? created`).

**Router (`routers/review.py`, new) + wiring.** `GET /review?status=&kind=` → list;
`POST /review/{id}` (id typed `uuid.UUID` → `422` on garbage, matching the activity router) →
`404` unknown / `409` already-resolved / `400` bad body for the kind. `NodeWriter` + `PgIndexStore`
are now hoisted in `main.py` and **shared** between the capture pipeline and the review service. New
setting `review_list_max` (200, rule 9). Contract-doc fields (`GET /review` returns `id/kind/payload
/excerpt/source/source_ref/created_at`) are all present; the response adds `status`/`resolution` (a
harmless superset the admin list uses).

**Verification.** 25 new unit tests (service resolve/list with fakes + a real `NodeWriter`; the
router status-code mapping; `append_edges`/`add_edges`; the resolver `pending_edges` enrichment). A
**real migration-005 pgvector smoke** exercised the new `PgReviewQueue` SQL end-to-end (enqueue →
list/kind-filter → get with `pending_edges` round-trip → guarded resolve → post-resolve list),
confirming the uuid-column str round-trip, jsonb encode/decode, and the pending-only guard. The full
capture→review→resolve→materialize→index flow end-to-end (live LLM) is part of the M3 Accept (task
10). **Independent structured review** (diff vs 08 §M3 + ADR-030/027 + 03-api + CLAUDE.md): no
must-fix findings — contract, layering, never-lose/visible, and store-is-truth all hold.

**Follow-ups:**
- **Task 7 (vocab surface)** consumes the queued `vocab-consolidation` markers (or the resolved
  `vocab-proposal` items) to mutate the live node/edge-type vocabulary + run the retro-consolidation
  walk. `GET /types` / `PUT /settings/vocabulary` land there too.
- `maybe` is terminal via the guarded transition (can't be re-resolved through this endpoint);
  M6's polished review UX revisits deferral/reopen if needed.

## Task 6

**Entity services — merge propose/apply + tombstones, backfill scan, tiered profile-refresh** —
server commits `f5795fe` (merge + migration 006), `9e544c3` (backfill + profiles), `f6f0678`
(review follow-ups), local, **not pushed**. 294 unit tests pass (18 new), `ruff check` green.

The three ADR-030 §4/§5/§6 entity-lifecycle services. A shared `entities/entity_store.py`
(`PgEntityStore`) centralizes the DB reads all three need (node lookup, inbound canonical edges,
entity listing, `entities_touched_since`, 1-hop neighborhood, alias-substring match) — every read
excludes tombstones (`merged_into` set). All three keep the store as truth (rule 1): they rewrite
**files** and let the indexer materialize the DB, never writing the graph tables directly.

**Merge (`POST /admin/entities/merge`, ADR-030 §5).** Two-step, ADR-024 shape. `MergeService.propose`
(sync, no writes/LLM) validates the pair (self-merge → 400, unknown → 404, tombstone endpoint /
non-entity → 400) and returns the inbound-edge inventory. `apply` opens an `agent="entity-merge"`
run and, in the background (202 `{run_id}`): retargets every inbound canonical edge loser→survivor on
the **source files** (new pure `retarget_edges` in `graph/node_writer.py` — redirects `to:`, drops the
now-duplicate edge so the pk survives reindex), **unions** the survivor's aliases with the loser's
name + aliases (`merged_alias_union` + `upsert_frontmatter_list`), writes the loser's permanent
**tombstone** (`render_tombstone` — keeps only `id`/`type`/`merged_into`, same path so old locators
resolve), reindexes the touched files, and **force-commits** (`backup_now`) — immediate, git history
is the checkpoint (no agent-window wait). The tombstone 302-redirect + search/neighbor exclusion were
already in place (task 5). Per-file failures are skip-and-continue (rule 7).

**Backfill scan (nightly, ADR-030 §6).** `entities/backfill.py` (`agent="entity-backfill"`, 04:20):
entities touched since the last successful run (watermark = its `started_at`; first run = the
`BACKFILL_WINDOW_DAYS` window) are re-checked against recent memory nodes whose chunk text contains a
**qualifying alias** (length ≥ `ENTITY_ALIAS_MIN_FUZZY_LEN` — ADR-032 entropy guard; shorter aliases
never scanned) and that lack an edge to the entity. A match auto-adds an `involves` edge on the memory
file (feed-flagged in the run), then reindex + force-commit. `NOT EXISTS`-edge exclusion + the
watermark make re-runs duplicate-free; `add_edges` now returns whether it actually wrote so the link
count/budget aren't double-spent when one memory matches two aliases of the same entity.
**M3 scoping (recorded):** the ADR-030 §6 "below-threshold → review item" branch is deliberately
**narrowed** — a coarse substring backfill would file noisy review items on short aliases, so those
are skipped, not reviewed. The richer review-generating drain (inbox re-organization + dedup
proposals) is M6 (04 §4 (d)/(c)); this M3 job ships the high-confidence auto-link half.

**Profile-refresh (nightly, ADR-030 §4 / ADR-034).** `entities/profile_refresh.py`
(`agent="profile-refresh"`, 04:10) + pure `entities/profiles.py`. For every entity-like node: compute
the 1-hop neighborhood → **tier by graph degree** (`stub` < `PROFILE_TIER_SNAPSHOT_MIN`(3) ≤ `snapshot`
< `PROFILE_TIER_FULL_MIN`(8) ≤ `full`, ADR-034) → regenerate only when the neighborhood **hash**
changed (idempotency + structurally caps nightly LLM spend). Stub = mechanical categorized observation
lines carrying `(as of …)` stamps, **no LLM**; snapshot/full = an LLM synthesis (observations injected
as delimited DATA — titles/rels/dates only, never bodies; injection hygiene). The profile text is
embedded and stored. Best-effort (rule 7): LLM down → store the stub text + `stub` tier + cleared hash
so the next run retries; embed down → store without a vector. `GET /nodes/{id}` now serves `profile`
from `node_profiles` via a LEFT JOIN in the search store's `get_node` (one query, was stubbed `null`).

**Migration 006 + storage decision (implementer's — docs delegate it).** 02-data-model §3 leaves the
derived-profile storage "an implementation choice within the derived tier". Chosen: a dedicated
**`node_profiles`** table (`node_id` PK/FK cascade, `tier`, `profile` text, `observations` jsonb with
supporting node ids, `neighborhood_hash`, `embedding vector(768)`, `refreshed_at`) — a separate table
(not a `nodes` column) keeps the indexer the sole writer of `nodes`/`chunks` and the profile-refresh
job the sole writer of profiles (clean ownership); fully rebuildable (a wiped table repopulates on the
next refresh; a reindex doesn't touch it). `compute_head` advances to `006`.

**Wiring.** All three services on `app.state` + `main.py`; merge behind `get_merge_service` + the admin
router endpoint; the two nightly jobs registered in the scheduler (optional params) and the CLI
(`python -m app.cli profile-refresh` / `entity-backfill`). Shared `StoreCommitter` protocol lifted to
`store_backup.py`.

**Verification.** 18 new unit tests over pure logic (retarget/tombstone/alias-union; tier selection,
observation rendering, neighborhood-hash stability) + the services against fakes + a real `NodeWriter`
(merge rewrites, backfill auto-link + no-double-count + watermark, profile tiering/skip/degrade) + the
merge router status-code mapping. **Not yet run: a real migration-006 pgvector smoke** over the new SQL
(the `entity_store` queries, `profile_store` upsert, the `node_profiles` LEFT JOIN, migration 006
up→down→up) — unlike tasks 1/4/5 this was left to the M3 Accept (task 10) alongside the deferred
live-LLM end-to-end flow. **Independent review** (fresh agent, diff vs 08 §M3 + ADR-030/034 + 02/03/04
+ CLAUDE.md): **no must-fix findings** — merge/backfill/profile correctness, the contract, and the
invariants (store-is-truth, never-lose, never-crash, idempotency, config-via-settings) all hold. 5
minors: 3 fixed inline (`f6f0678` — backfill double-count; degraded-profile tier/content agreement;
stored-observation bound), 2 logged ↓.

**Follow-ups:**
- **Profile embedding not yet surfaced in search** (reviewer + design): `node_profiles.embedding` is
  populated but nothing reads it — an entity's current profile doesn't yet appear as a search hit
  ("searching Alex hits the summary", ADR-030 §4). Wire the profile embedding into search/`similar`
  (a small SQL union) at task 8/10 or with the M4 retrieval work.
- **Real migration-006 pgvector smoke** over the new SQL — run at/before the task-10 Accept (as the
  live-LLM flow is), verifying migration 006 up→down→up and every new plain-SQL path against a real DB.
- **Merge/backfill source-file rewrites aren't serialized under the store lock** (reviewer #4): each
  file write is atomic, but two concurrent applies (or an apply racing a capture) touching the *same*
  source file can lose a retarget (last-writer-wins). Matches the existing tag-consolidation-apply
  precedent (the store lock guards git only) and git history is the revert net, so it's a latent gap on
  infrequent manual admin ops, not a functional bug — revisit if apply/capture concurrency tightens.
- **Alias accretion** (the combined-review follow-up carried from tasks 2/3/5): resolution still does
  not append a newly-met surface form to a matched entity's `aliases`, so Alex/Alexandru across
  *organize* passes still mints two nodes until merged. Backfill now links *memories* to entities by
  alias, and merge collapses the two hubs, but the accretion-on-link half (+ the organizer emitting
  aliases) remains open — wire before the task-10 Accept.

## Task 7a

**Vocabulary governance core — `GET /types`, `PUT /settings/vocabulary`, forward-live effective
vocab** — server commit `dd3c5be`, local, **not pushed**. 317 unit tests pass (+23), `ruff check`
green. Scope split recorded at kickoff ([08 §M3](../08-implementation-plan.md) task 7 /
[ADR-035](../adr/035-vocabulary-consolidation-scope-m3.md)): **7a = the forward-live half**
(governance + approval makes a type live + a feed-visible consolidation run); **7b = the edge
retro-consolidation apply** (deferred). The retro-consolidation *mechanics* were under-specified in
ADR-027 §3 — grilled at kickoff, recorded as ADR-035 **before** coding (09 discipline), then built.

**Approved-vocabulary store (`vocab/store.py`).** New `PgVocabularyStore` over the existing
`app_settings` table (its first user), one `"vocabulary"` key → jsonb `{node_types, edge_rels,
entity_like_types}` = the user-approved **additions** per axis. `get_additions()` + `add()`
(read-modify-write in a `FOR UPDATE` txn → upsert; dedup, order-preserving, idempotent — re-approving
a type is a no-op). Pure decode/merge helpers unit-tested; the SQL itself needs a real-DB smoke (↓).

**Effective vocabulary (`vocab/service.py`).** `EffectiveVocabulary` = config **seeds ∪ approved
additions** per axis (02 §3 "config + app_settings"). `effective_vocabulary(vocab, settings)` is the
one accessor every writer reads through; a `None` provider falls back to the seeds, so the ~6
services that read vocabulary gained an **optional** `vocab` param and existing tests construct
unchanged. Threaded into: the organizer prompt + `validate_organizer_output` (capture pipeline), the
entity **resolver** (entity-like filter), the nightly **backfill**/**merge**/**profile-refresh**
(entity-like listing), and the review-queue **mint** (a `new`-entity of a freshly-approved type is
now accepted). Result: an approved type is **forward-live at once** everywhere, not just in `/types`.

**The choke point (`VocabularyService`).** `list_types()` serves `GET /types` (effective vocab +
pending `vocab-proposal` items). `resolve_proposal(review_id, verdict)` is the **one** approve/reject
path behind both `PUT /settings/vocabulary` and `POST /review/{id}` (ADR-027 §4): approve writes the
type to `app_settings` (`node_type`→node_types; `entity_type`→**both** node_types + entity_like_types,
so `ENTITY_LIKE_TYPES ⊆ NODE_TYPES` holds for the effective sets; `edge_rel`→edge_rels), opens the
`vocab-consolidation` run, then flips the item `resolved`; reject discards. Mutation happens **before**
the run + status transition, so a failure leaves the item pending (retryable) — and every step is
idempotent. The **Review service delegates** its whole `vocab-proposal` branch here (it owns its own
transition); the three resolution exceptions moved to `review_queue` (shared, no service
cross-import), re-exported from `review_service` for the router.

**Consolidation run (`vocab/consolidation.py`, 7a slice).** `VocabConsolidation.start()` opens a
`SUCCEEDED` `vocab-consolidation` `agent_runs` row recording the now-live mutation (vision P8) —
replacing task 4's `SKIPPED` queued marker. Best-effort (rule 7): the vocab is already committed, so
a run-store hiccup never fails the approval. **7b** hangs the edge re-walk propose→apply off this same
service + agent name.

**Endpoints + wiring.** `GET /types` on the meta router; a new `routers/settings.py`
(`PUT /settings/vocabulary`) — the first `Settings` endpoint (`GET /settings` + `PUT /settings/models`
arrive with M4). `TypesResponse`/`VocabProposalItem`/`VocabularyResolveRequest` models; the 404/409/400
mapping matches `POST /review/{id}`. `VocabularyService` (+ `PgVocabularyStore` + `VocabConsolidation`)
constructed on `app.state` right after the review queue and threaded into every writer.

**Verification.** 23 new tests: the effective-vocab union + forward-live-after-approve, `list_types`,
`resolve_proposal` across all three axes + reject + every error shape + idempotency, the store's pure
decode/merge, and the two routers (`GET /types` passthrough; `PUT /settings/vocabulary`
approve/reject/404/409/400/422) — all over fakes (a real `VocabularyService` in the review-service
test proves the delegation). Full suite **294 → 317**, `ruff` green. **Self-reviewed** against the
Accept + ADR-027/035 + CLAUDE.md invariants (store-is-truth: additions are operational state, never
files; one governance choke point; config-via-settings; idempotency; rule-7 best-effort run) — no
issues found.

**Independent review** (fresh agent, diff vs the Accept + ADR-027/035 + 03-api + CLAUDE.md):
**APPROVE — no must-fix findings**. It verified the `resolve_proposal` ordering + all five error
shapes (both routers), the `entity_like ⊆ node_types` invariant for the effective sets, the single
choke point / no double-transition, store-is-truth (approvals touch only `app_settings`; folders are
lazily created by the writer, so the seeds-only boot skeleton is correct not a leak), SQL safety, the
rule-7 best-effort run, no stray seed reads, and clean 7b scope discipline. 4 minors, 2 addressed in
`410b5d2`: (1) the review-service **mint forward-live** claim was untested → added
`test_mint_accepts_a_freshly_approved_entity_type` (approve `entity_type` "pet" → minting a `new`
pet succeeds, having first rejected pre-approval); (2) documented the first-creation `FOR UPDATE`
race in `PgVocabularyStore.add` (harmless single-user). 2 logged ↓. 318 tests, ruff green.

**Follow-ups:**
- **Task 7b** — edge retro-consolidation apply: `NodeWriter.retype_edge`, the LLM re-walk propose +
  `POST /admin/vocab/consolidate` confirm-apply (edges only); node re-typing stays propose-only
  (folder-move/re-slug/`node_paths` apply machinery = its own ADR + task, ADR-035).
- **Duplicate consolidation run on approve-retry** (reviewer minor): the `SUCCEEDED` run is opened
  before the final review-item transition, so if that transition raises and the user retries, a
  second "type is now live" run appears in the feed. Cosmetic (the run is best-effort, the mutation
  is idempotent); revisit if it ever matters.
- **Real migration/pgvector smoke** of the `PgVocabularyStore` SQL (the `app_settings` SELECT +
  `FOR UPDATE`→upsert round-trip) — run at/before the task-10 Accept, with the other deferred smokes.

## Task 7b

**Edge retro-consolidation apply — `POST /admin/vocab/consolidate`** — server commit `542459d`,
local, **not pushed**. 344 unit tests pass (+26), `ruff check` green. The edge half of ADR-027 §3's
retro-consolidation, **scoped at kickoff by [ADR-036](../adr/036-edge-retro-consolidation-walk-retypings-only-m3.md)**
(recorded before coding, 09 discipline): the walk **re-types existing edges only** — inventing
brand-new edges from node bodies is deferred, node re-typing stays propose-only (ADR-035) — via an
**on-demand two-step** endpoint mirroring ADR-024 tags (the 7a approve-time marker run unchanged).
ADR-036 also reconciled the ADR-035 §2 / 04-pipelines wording split (same-commit `a528eba` — loose
phrasing of one intent, not a supersession).

**`retype_edge` (pure, `graph/node_writer.py`).** Twin of `retarget_edges` but rewrites an edge
line's **`rel`** (keyed by `to` + old `rel`) instead of its `to`. Preserves `conf`/`since`/`until` (a
re-typing is not a re-dating), touches only the `edges:` block, dedups against an identical existing
edge so the `(src,dst,rel,origin)` pk can't be violated on reindex, and returns the file verbatim on
no-match (no newline churn). **Field-boundary match** (`_rel_field_at`) so `rel: at` can't corrupt
`rel: at_home` — rels are variable-length slugs, unlike the fixed-format uuids `retarget_edges` keys
on (independent-review must-fix). Atomic `NodeWriter.retype_edge` wraps it.

**Bounded edge inventory (`vocab/edge_store.py`).** New `EdgeConsolidationStore`: `edge_inventory`
returns up to `vocab_consolidate_max_edges` (config, default 300) canonical edges — src/dst titles +
current rel + a chunk-0 source excerpt — **most-recent-source first** (recency window over a large
graph, ADR-036 §1), excluding the target rel and tombstoned endpoints; `store_paths_for` resolves
the chosen sources' paths **server-side from the DB** (apply never trusts a client path).

**Service (`vocab/edge_consolidation.py`).** Pure helpers (prompt render, `parse_retype_plan`,
`retypes_from_indices`, `clean_retypes` — drop no-ops/dupes) + `EdgeConsolidationService`. `propose`
validates the rel ∈ effective edge rels (400 else), feeds the inventory to the distill chain, and
maps model-returned indices → sanitised `{src_id, to, from_rel, to_rel}` (no writes; provider-down →
503; empty inventory → no model call). `apply` re-sanitises the reviewed plan (forcing `to_rel` to
the validated rel — a client can't re-type onto an arbitrary rel), opens a `vocab-consolidation` run,
and in the **background** re-types each edge → reindexes touched files → force-commits (202
`{run_id}`). Rule 7: per-edge failures skip-and-continue, any unexpected failure ends the run
`failed`; store is truth (files rewritten, indexer re-materializes the DB), so a partial apply is
re-drivable. Same agent name as the 7a marker (the log foretold this).

**Endpoints + wiring.** `POST /admin/vocab/consolidate` on the admin router (propose 200 / apply
202; **400** unknown/empty rel — no schema `min_length`, the service owns it — or empty apply plan;
**503** distill down), `VocabConsolidate*`/`EdgeRetypeItem` models, `PgEdgeConsolidationStore` +
`EdgeConsolidationService` on `app.state`, `drain()` into the lifespan shutdown.

**Verification.** 26 new tests: `retype_edge` (rel-only rewrite + metadata preserved, dedup,
prefix-collision boundary, verbatim no-match, atomic writer); pure parse/sanitise (fence tolerance,
bool-not-index, out-of-range drop, no-op/dupe drop, one-line excerpt, untitled fallback); service
propose (sanitise, skip-model-when-empty, provider-down, unknown-rel) + apply (rewrite→reindex→
commit, two edges one file reindexed once, skip-unresolvable-source, no-op-plan-no-commit,
unknown-rel); router (200/202/400 empty-plan + empty-rel/503). Full suite **318 → 344**, ruff green.

**Independent review** (fresh agent, diff vs ADR-035/036 + 03-api + CLAUDE.md): **APPROVE after
fixes.** 1 must-fix — `retype_edge`'s substring `rel:` match could corrupt a prefix-colliding rel
(`at` vs `at_home`), silent frontmatter corruption; fixed with the field-boundary `_rel_field_at`
guard + a regression test. 3 minors, all applied: inventory now recency-ordered (the cap was
alphabetical → a large graph's tail was invisible); empty `rel` now 400 not 422 (dropped schema
`min_length`); per-edge `except` widened `FileNotFoundError` → `Exception` (matches tags + the
docstring's skip-and-continue promise). Nits (to_rel-override comment, same-file test) also addressed.

**Follow-ups:**
- **New-edge invention on consolidation** (ADR-036 deferred): an organize-style body re-read that
  proposes edges the graph never captured but that should use a rel — its own task (post-M3).
- **Node re-typing apply** (ADR-035 deferred): folder-move / re-slug / `node_paths` machinery + the
  node-type consolidation walk's apply half.
- **Real-DB smoke** of the `PgEdgeConsolidationStore` SQL (the recency-ordered canonical-edge
  inventory join + `store_paths_for`) — run at/before the task-10 Accept, with the other deferred
  smokes.

## Task 8

**Web retarget — the PWA from the note model to the typed graph** — server repo `web/` commit
(local, **not pushed**). `tsc --noEmit` + `eslint --max-warnings 0` + `vite build` all green.
Retargets the five surfaces the plan lists (08 §M3 task 8) against the M3 API (03-api §Search &
graph / §Review / §Settings); no server code touched (ADR-006 — web knows only the API base URL +
hand-kept types).

**API layer (`api/types.ts`, `api/client.ts`).** Wire types retargeted note→node: `SearchResultItem`
(`note_id/vault_path`→`node_id/store_path` + `type`), new `NodeDetailResponse` (+`NodeEdgeItem`,
`aliases/disambig/occurred/occurred_end/profile/edges`) replaces `NotePreviewResponse`/
`RelatedNoteItem`; `CaptureView.note_paths→node_paths`; `HealthResponse.vault→store` (+`backups`).
New `TypesResponse`/`VocabProposalItem`, `ReviewItemResponse`/`EntityCandidate`. Client:
`getNote`→`getNode` (`/notes/{id}`→`/nodes/{id}`), `search` gains `types`, plus `types()`,
`listReview`/`resolveReview`, `resolveVocabulary`. Field-checked against `server/app/models.py` +
the resolver/vocab payloads.

**Shared (`ui/nodeTypes.ts`).** `typeIcon`/`typeLabel`/`edgeLabel` — an emoji map over the 9 seed
types with a `?? fallback` glyph so a *governed* addition the web has never seen still renders
(ADR-027: the vocabulary is extensible; no web-side hardcoded list is treated as complete).

**Search (`SearchScreen.tsx`, `useSearch.ts`).** Node cards carry the **type icon**; plane **and**
type filter chips (type chips sourced from `GET /types`). Expand → **node preview** (`GET
/nodes/{id}`): identity line (disambig + aliases), the derived **profile** block (entity nodes),
body, **Connections** (canonical edges — rel label + direction arrow + target type icon) and
**Similar** (derived edges, faint). Edge chips are display-only jump-off points; the Map makes them
navigable at M7 (06 §5).

**Capture (`RecentCaptures.tsx`).** The recent strip renders `node_paths` as typed chips
(`typeIcon(folder)` + slug with the `--<shortid>` suffix stripped); the `written` status label
de-noted to "Writing nodes".

**Review (`features/review/*`, new tab).** The M3 minimal, kind-generic queue (06 §3b; polished UX
= M6). `entity-ambiguity` → candidate picker (each candidate's name/disambig/aliases; pick / **New
entity** / **Not sure — later** → `POST /review/{id}` `{choice}`); `vocab-proposal` → Approve/Reject
(`{verdict}`); unknown future kinds render inertly rather than hide. Defensive payload readers over
the untyped `payload`.

**Settings → Vocabulary (`features/settings/VocabularyPanel.tsx`, `useVocabulary.ts`).** Node-type
(icons; entity-like types outlined) + edge-rel lists from `GET /types`, and pending proposals with
Approve/Reject via `PUT /settings/vocabulary`. Shares the `['types']` query key with the search
type chips, and both vocab-resolve paths invalidate `['types']` + `['review','pending']` so the two
surfaces stay in sync (same server choke point, ADR-027 §4).

**Copy retargets.** Admin card descriptions de-noted (graph store / nodes / canonical+derived edges);
Chat placeholder milestone fixed M3→**M4** (the pivot moved chat) + "notes"→"memories"; Activity
placeholder → **M8** (ops-console restructure).

**Verification.** No web unit tests exist (testing policy is server-side; M0–M2 web tasks verified
by build). Beyond green tsc/eslint/vite: a **real browser walkthrough** against a throwaway mock of
the M3 API (canned graph data, vite proxy repointed) — drove all five surfaces authenticated,
console clean. Confirmed: search node cards (type icons 👤🧠💡, plane badges, scores); node preview
(Profile with `(as of …)` stamps, Connections `← INVOLVES 🧠` / `→ AT 📍`, Similar `← SIMILAR 💡`);
Review both kinds, with `POST /review/{id}`→refetch observed on the network log; Settings→Vocabulary
lists + `PUT /settings/vocabulary` observed; Capture strip typed `node_paths` chips. The live
capture→node→search flow against the real stack (Postgres migs 005/006 + Ollama + providers + graph
store) is the **M3 Accept (task 10)**, as for the deferred backend smokes.

**Independent review** (fresh agent, diff vs 08 §M3 task 8 + 03-api + 06-web-app + CLAUDE.md, cross-
checked against the actual `server/app/models.py` + resolver/vocab payloads): **APPROVE-WITH-MINORS —
no must-fix**. It verified every field name + endpoint + method matches the server, no server-import
leak, defensive payload reads, and the cross-invalidation coherence. 3 minors, all resolved: (1) the
`written` label was still "Writing notes" (pivot-vocab leftover) → "Writing nodes"; (2) preview now
renders `disambig`/`aliases`; (3) `HealthResponse` gained the server's `backups` field. tsc/eslint
re-green after the fixes.

**Follow-ups:**
- **Profile-embedding-in-search + alias-accretion** (carried from tasks 5/6) remain server-side
  open-before-Accept items; nothing web-side blocks on them.
- **Edge-consolidation apply UI** (`POST /admin/vocab/consolidate`, task 7b) and **entity-merge UI**
  (`POST /admin/entities/merge`, task 6): both endpoints exist without a web surface — deliberately
  **out of task-8 scope** (not in the plan's task-8 list; admin/ops-console territory, M8). Approving
  a type still opens the 7a marker consolidation run; the on-demand edge re-walk apply has no button
  yet.
- **NodePreview edge chips are display-only** — navigable expansion is the Map (M7, 06 §5).

## Task 9

**Deploy/CI retarget vault → graph store (2026-07-14).** The app-level graph-store config +
bootstrap landed in tasks 1/6 (`graph_store_path`/`graph_store_repo`, `StoreBackupService.ensure_ready`
= init + remote-wire + skeleton + `push -u`); task 9 renamed the **deploy layer** to match and added
the cutover convenience. Deploy-only — **no server/web code touched**, so no unit tests exercise it
(deploy layer isn't unit-tested; verification is syntax + wiring trace + independent review).

**Changes (commit `e97671b`, code repo, not pushed):**
- `deploy/defaults.env`, `deploy/.env.example`: `VAULT_PATH=/srv/vault` → `GRAPH_STORE_PATH=/srv/graph-store`;
  **added `GRAPH_STORE_REPO=git@github.com:bodyionita/PSB-graph.git`** (the app wires this remote +
  `push -u` on first boot — ADR-031 §6). Comments updated; two intentional "renamed from VAULT_* at M3"
  historical notes kept.
- `deploy/docker-compose.yml`: store bind `/srv/graph-store:/srv/graph-store`; deploy-key bind
  `/home/deploy/.ssh/graph_store_deploy_key:/run/graph_store_deploy_key:ro`.
- `deploy/docker-entrypoint.sh`: `KEY_SRC=/run/graph_store_deploy_key`, installs to
  `/root/.ssh/graph_store_deploy_key`, `core.sshCommand` points at it (GitHub host-key pin unchanged).
- `deploy/Dockerfile`: `git config --global --add safe.directory /srv/graph-store` — this was a
  **functional miss** (the old `/srv/vault` would have left the new mount "dubious ownership" for the
  root-run container's git remote probe + auto-backup). Comments retargeted.
- `deploy/provision.sh`: `VAULT_DIR/REPO/DEPLOY_KEY` → `GRAPH_STORE_DIR/REPO/DEPLOY_KEY`
  (`/srv/graph-store`, `PSB-graph`, `graph_store_deploy_key`), `VAULT_READY` → `GRAPH_STORE_READY`;
  the two-pass SSH-hardening guard (defer hardening until clone succeeds so root stays reachable across
  the deploy-key bootstrap) is preserved. Noted that an **empty PSB-graph clones fine** into an empty
  tree and the app then seeds the skeleton + `push -u` — so DR-restore (populated repo) and the
  zero-touch cutover (empty repo) share the one clone path.
- `.github/workflows/ci.yml`: **new deploy-workflow step** (ssh-action, gated on `HAVE_VPS`) that prints
  the VPS's graph-store deploy **PUBLIC** key (`~/.ssh/graph_store_deploy_key.pub`) into the Actions log.
  Best-effort (both branches exit 0 — never fails the deploy); private key never leaves the box (ADR-016).
  Idempotent (reprints each deploy). This is the ADR-031 §6 "print the pubkey into the Actions log so the
  user's whole manual surface is GitHub UI" — a convenience now that the key is already registered.

**Verification.** `bash -n`/`sh -n` clean on the two shell scripts; YAML parse clean on `ci.yml` +
both compose files. Config reachability traced: `config.py` (`case_sensitive=False`, no prefix,
`env_file .env`) maps `GRAPH_STORE_PATH`/`GRAPH_STORE_REPO` from the container env; the deploy job
`cp`s `defaults.env → .env` (secrets appended) and the api container reads it via `env_file: [.env
raw]`. No compose-interpolation hazard: the repo URL holds no `$` and is never referenced as
`${...}`. Live exercise is the **M3 Accept (task 10)** — a real deploy that prints the pubkey and
drives capture→node→PSB-graph push.

**Independent review** (fresh agent, diff vs 08 §M3 task 9 + ADR-031 §6 + 07-infra + 09 secrets
discipline + CLAUDE.md, wiring traced against `config.py` + `store_backup.py`): **APPROVE — no
must-fix**. Verified rename completeness (only the two allowed historical comments remain), five-way
path agreement (`/srv/graph-store`, `/run/graph_store_deploy_key`, `/home/deploy/.ssh/graph_store_deploy_key`),
the defaults→.env flow, pubkey-print prints only the public half and is best-effort, secrets rule 11
clean, and the two-pass guard intact. One **informational** (outside the diff): the local untracked
`server/.env` still had `VAULT_PATH=../ObisidanVault` (silently dropped by `extra="ignore"`) — updated
to `GRAPH_STORE_PATH=../graph-store` for dev clarity (not committed; gitignored).

**Open before Accept (unchanged, carried to task 10):** real-DB SQL smoke of the task-6/7a/7b SQL,
profile-embedding-in-search, cross-capture alias accretion. Task 9 adds no new backend surface to smoke.

## Task 10 (in progress) — live M3 Accept

**Session start (2026-07-14).** Implementation session against task 10. Established the green
baseline (344 tests, ruff clean) and drove the carried backend smokes against the **real local
pgvector** DB (`braindan-dev-db`, `pgvector/pgvector:pg16`):

- **Migration 006/007 real-DB apply.** Local DB was at `005`; `alembic upgrade head` applied `006`
  (`node_profiles`) cleanly. (007 — the profile HNSW index — is authored as part of the profile-in-
  search build below.)
- **Real-DB SQL smoke (`scripts/smoke_db.py`).** A standalone script drives the *actual* `Pg*` store
  classes against the running Postgres with deterministic 768-vectors (what the unit tests fake),
  isolated by fixed test UUIDs + a cleanup `finally` (dev data untouched). **23/23 checks green**
  across: `PgAliasStore` (exact title + normalized-alias recall, type filter, tombstone exclusion —
  the entity-resolution query), `PgEntityStore` (task-6 merge reverse-index join, backfill alias
  scan, `neighborhood` with since/until, `entities_touched_since`, tombstone/derived exclusion),
  `PgProfileStore` (upsert + ON-CONFLICT update + pgvector codec), `PgSearchStore` (get_node profile
  LEFT JOIN, `search_chunks`), `PgVocabularyStore` (task-7a `app_settings` jsonb, idempotent add),
  `PgEdgeConsolidationStore` (task-7b bounded inventory, exclude-rel, chunk-excerpt join, path
  resolution). This closes the task-6/7a/7b "real-DB smoke" open items in one runnable artifact.
- **Alias accretion.** The *recall* side is proven (normalized `alexandru` → the Alex node when
  already an alias). True cross-capture accretion (recording a **new** surface form onto a matched
  entity) stays a documented M3 follow-up (fuzzy/accretion, per the task-3 note); the Accept's
  "mentioned twice → one node" is served by the exact-alias short-circuit on a repeated surface form.

**Replan: profile-embedding-in-search ([ADR-037](../adr/037-profile-embedding-in-search-m3.md)).**
The smoke confirmed a real gap: `search_chunks` queries `chunks` only, so the profile vector stored
in `node_profiles.embedding` (task 6) is **never read** — contradicting [ADR-030 §4](../adr/030-entity-substrate-and-lifecycle.md)
("embedded — searching 'Alex' hits one dense, current summary"). Per protocol this switched to a
replanning pass (grilled decision-by-decision). Decisions, all recorded in ADR-037: a **second
per-profile vector leg** unioned `UNION ALL` with the chunk leg, `DISTINCT ON (node_id)` best-per-
node (free cross-leg dedup); **all tiers** eligible (`embedding IS NOT NULL`, `merged_into IS NULL`);
**no leg weighting** (raw cosine union — RRF is M4); **`SearchResultItem` unchanged, no marker
field, no web change** (profile hit = entity card, profile text as snippet); **reindex stays
decoupled** from profiles (plain reindex preserves them via stable file-sourced ids; full DB wipe →
profile-search degraded until `profile-refresh` reruns — rule 1 restores from the store, profiles
aren't in it); **migration 007** adds `hnsw (vector_cosine_ops)` on `node_profiles.embedding`. Docs
updated in the same change set: 02-data-model (migrations 006/007 + profile-as-retrieval-source),
03-api §Search, 04-pipelines §4/§5, this log.

**Live-Accept decision:** **go straight to prod** (no local full-stack dry-run) — push code → CI
deploy to `braindan.cc` → run the Accept on prod → user archives `PSB-vault`. Sequenced *after* the
profile-in-search build + independent review.

**Build — profile-embedding-in-search (commits `d0fc52e` + `8ec472a`, code repo, not pushed).**
- `migrations/versions/007_node_profiles_hnsw.py` — `CREATE INDEX … USING hnsw (embedding
  vector_cosine_ops)` on `node_profiles.embedding` (idempotent `IF NOT EXISTS`), mirroring
  `chunks_embedding_hnsw`. Applied to the local DB; head `006 → 007` (migration-head test updated).
- `app/search/store.py` — `search_chunks` grows the profile `UNION ALL` leg over `node_profiles`
  (join to `nodes`, `embedding IS NOT NULL` + `merged_into IS NULL` + plane/type filters), wrapped in
  `SELECT DISTINCT ON (node_id) … ORDER BY node_id, dist` so the nearest of {best chunk, profile}
  wins per node; outer `WHERE score ≥ min_score ORDER BY score DESC LIMIT top_k` unchanged. Raw
  cosine, no leg weighting. `SearchStore`/`SearchService`/`SearchResultItem` signatures untouched;
  only caller is `search/service.py`.
- `scripts/smoke_db.py` — the real-DB SQL smoke promoted into the repo (isolated test UUIDs +
  cleanup `finally`, refuses a non-local DSN). **26 checks green** vs local pgvector, incl. the ADR-037
  anchors: a profile-only entity (no chunk) now surfaces via search with the profile text as snippet,
  the type filter applies to the profile leg, and a **tombstoned** entity's profile is excluded.
- **344 unit tests green, ruff clean.**

**Independent review (self-review vs ADR-037 + M3 acceptance criteria + CLAUDE.md rules 1/5/9 +
02/03/04 contract): APPROVE — no must-fix.** Verified UNION column parity, the best-per-node dedup,
every ADR-037 guard, invariant cleanliness (plain SQL, profiles DB-only, config-driven limits, no
secrets), and that the sole caller + the fake-store unit tests are unaffected. **1 minor fixed**
(`8ec472a`: explicit tombstone-exclusion smoke assertion). **1 informational** (pre-existing, not
introduced): `UNION`+`DISTINCT ON` prevents a clean HNSW ANN top-k — acceptable at M3 scale, the
chunk query already had the `DISTINCT ON` shape, and the index gives parity + future value.

**Status — carried backend smokes CLOSED:** task-6/7a/7b real-DB SQL ✓ (smoke), migration 006/007
real-DB apply ✓, profile-embedding-in-search ✓ (built + smoke). Alias *accretion* (new surface form
onto a matched entity) remains the documented M3 follow-up. **Remaining for the Accept:** the live
prod cutover — a push to `main` auto-deploys (CI runs `alembic upgrade head` on the prod Supabase DB;
migration 005 drops the pre-pivot note tables) → app bootstraps `PSB-graph` → drive capture→node→push
< 30s, entity resolution, `inbox/` fallback, vocab-approve consolidation, DB-wipe/reindex edge
parity, `profile-refresh` + profile-search, threshold-tune `ENTITY_MATCH_MIN_CONF` → user archives
`PSB-vault`. This is a joint live session (voice capture, tuning, the GitHub-UI archive).

**Prod cutover done (2026-07-14).** Pushed code (`afdfd8f..8ec472a`) to `main`; CI green
(test/web/gitleaks/deploy). Deploy step ran `docker compose up -d --build` + `alembic upgrade head` on
prod — migrations 005 (drop note tables → graph pivot) / 006 / 007 applied. `GET /api/v1/health` →
`{db,store,git_remote,backups}` all true. Prod is live on the graph schema.

**Deploy-key gap (found + fixed during Accept).** First captures resolved locally but **nothing
reached `PSB-graph`** (repo stayed empty). Root cause: the graph-store deploy key was **missing on the
VPS** — `provision.sh`'s key-gen (`/home/deploy/.ssh/graph_store_deploy_key`) was never re-run post
task-9, so the compose bind (`docker-compose.yml`) mounted a non-existent path, the entrypoint set no
`core.sshCommand`, and the per-capture `push` **soft-failed** (`git_repo.push` keeps commits local on
auth failure — health stays green by design). CI's best-effort pubkey-print had already logged "key
not found". **Fix (user, on the VPS — key material never in chat):** reused the now-freed
`vault_deploy_key` renamed to `graph_store_deploy_key` at the bound path (cleared the empty dir Docker
created, `--force-recreate api` so the bind mounts the file), registered its pubkey on `PSB-graph`
deploy keys (write). Auth test → `Hi bodyionita/PSB-graph!`; the queued local commits pushed; per-
capture push now works. **Cutover-sequencing lesson:** the graph-store key must be provisioned on the
VPS *before* the first `docker compose up`, else the bind creates a dir that blocks later key-gen.

**Live Accept findings (drove the task-11 replan).** 4 captures landed with correct typed structure
(edges/disambig/aliases/planes/occurred/organizer_version) BUT:
- 🔴 **Dangling edges** — a `conversation` node's edges pointed at entity ids with **no node file**.
  Confirmed cause: the follow-up **reorganize** `remove_nodes(node_paths)` deletes the entity **hubs**
  the first pass minted (they're in `node_paths`), the reindex only indexes new files so the hub
  lingers in the alias index, and the fresh pass **links** to that now-fileless id → edge to nowhere.
- 🟡 **Over-extraction** — narrative memories emitted as `person`-typed content nodes ("How I know
  Horia") instead of thin hubs + a `memory`.
- 🟡 **Entity split** — "Horia" vs "Horia Fenwick" became two hubs: candidate retrieval is exact-alias
  only, so the variant never surfaced the existing hub for disambiguation (fuzzy/accretion deferred).
- 🟡 **Diacritics** — `_SLUG_INVALID`/`_TAG_INVALID` strip diacritics to `-` (Mădălina → `m-d-lina`),
  while `normalize_alias` keeps them → mangled filenames/tags + matching mismatch.
- 🟢 **`inbox/` clarified** — gibberish captures became `memory` nodes tagged `unclear` (graceful
  "could not interpret" body), NOT `inbox/`. Per rule 2, `inbox/` is the **model-failure** fallback,
  not a "content is nonsense" bucket — working as designed; the Accept step-3 wording needs a real
  pipeline failure to demonstrate (unit-tested instead).
Review queue was empty (no entity-ambiguity items), confirming the dangling edges are a bug, not
pending-by-design.

**Replan → task 11 (grilled 2026-07-14, ADR-038…042).** User chose "pause & replan quality" and set a
binding principle: **already-ingested data must survive bug fixes** (reprocess raw, never silently
drop; surface migrate-vs-delete) — lifted to **vision P10**. Eight decisions taken (all recorded in
the ADRs + 08 task-11 block): hubs never deleted by reorganize (+ tolerate orphans); entity types are
mention-only (+ coercion guard); token-overlap retrieval + alias accretion; fold diacritics on all
derived fields at the writer (raw kept); reusable `reprocess-all-from-raw` op preserving human
governance; bundle + local-test the reprocess before prod. Prod keeps the 4 defective captures until
the task-11 reprocess heals them (raw is safe; nothing new capturing). **No code written this session
after the cutover — paused for implementation.** Code committed through `8ec472a` and **pushed**.

## Task 11 (in progress) — organizer-quality + data-survival

**Implementation session (2026-07-14).** All five ADR-038…042 fixes built + unit-tested; **374 tests
(+30), ruff clean**; real-DB smokes green. Local, **not pushed**.

**Kickoff classification decision (user, grilled).** ADR-038/039 key off a config set they call
`entity_types` = person/place/event/project/topic, but the codebase had `entity_like_types` =
…+`idea` (idea was mintable as a hub, and the API `GET /types` exposes `entity_like_types`, which the
web reads — ADR-006). Surfaced the contradiction; the user chose **`idea` = content, align the
resolver**: `idea` is reclassified content-only (the resolver no longer mints idea hubs), realized by
**dropping `idea` from the existing `entity_like_types`** (default now
`person,place,topic,event,project`) rather than a wide/contract-breaking rename — so the one threaded
effective set *is* the ADRs' `entity_types`, used for the reorganize skip-filter, the coercion guard,
and the resolver mint set at once. No API/jsonb-key rename (web unaffected). 02 §2 updated (idea moved
to content; entity-hub list narrowed).

- **ADR-038 — reorganize preserves hubs.** `NodeWriter.remove_nodes(paths, *, keep_types)` skips any
  path whose top-level folder (= type) is an entity-hub type; returns the removed paths. The pipeline
  reorganize **and** retry paths pass the effective entity-hub set (`_entity_hub_types`), so a shared
  hub a capture minted is never deleted → no dangling edges; the fresh pass re-links to the live hub.
- **ADR-039 — coercion guard.** Organizer prompt (v5) states entity types are mention-only + content
  types are memory/idea/insight/conversation. `validate_organizer_output` now returns a 3rd tuple
  (coerced entity types) and **coerces any node whose canonical type is an entity type → `memory`**
  (body/title/tags/planes/entities kept), before resolution/write. Surfaced in the capture's
  `agent_runs` (`organize.coerced_entity_nodes`).
- **ADR-040 — token-overlap retrieval + accretion.** `EntityCandidate` gains `store_path` (for
  accretion, never sent to the LLM). `PgAliasStore.find_candidates(name, *, types, tokens, limit)`
  unions the exact leg with a **token-overlap** leg (`regexp_split_to_array(lower(surf),'\s+') && $tokens`
  over aliases+title) so a variant surface form surfaces the hub; `entity_min_token_len` +
  `entity_stop_tokens` are the low-entropy guard. The resolver computes significant tokens, and the
  **exact short-circuit fires only on a true exact hit** (`_is_exact`) — a fuzzy single candidate goes
  to the LLM (never a fuzzy auto-link). A confirmed link under a new surface form records an
  `AliasAccretion`; the pipeline (`_apply_accretions`) and the review service (`_accrete_on_link`)
  rewrite the hub's `aliases` (`NodeWriter.set_aliases`, folded) + reindex, feed-visible. Token-*prefix*
  (Alex/Alexandru) stays the documented fuzzy follow-up.
- **ADR-041 — diacritic folding.** New `app/text.py::fold_diacritics` (Romanian map + NFKD, combining
  marks stripped), the sole authority. Applied in `slugify`, `_slugify_tag`, `normalize_alias` (+ the
  alias index folds via the stored forms), and `NodeWriter.write_nodes`/`set_aliases` fold **every**
  derived field (slug/title/aliases/disambig/tags/body). Raw capture never folded.
- **ADR-042 — reprocess op.** `services/reprocess.py`: `ReprocessService` (confirm-preview + apply,
  single-flight, background `agent_runs`) + `PgReprocessStore` (`TRUNCATE nodes CASCADE` → clears
  chunks/edges/node_profiles; `TRUNCATE review_queue`; clear `captures.node_paths`; chronological
  capture ids; counts + tombstone-merge count reported). `CapturePipeline.reprocess_capture` re-ingests
  ONE capture from stored raw (combined text; **inbox-fallback on unusable organize**, unlike reorganize;
  awaited/sequential so accretion rebuilds chronologically). `NodeWriter.remove_all_nodes` resets the
  store files. `POST /admin/reprocess`, CLI `reprocess-all`, main/CLI wiring, `build_reprocess_service`.

**Verification.** 30 new unit tests: `fold_diacritics`; writer folding + `keep_types` + `remove_all_nodes`;
resolver token-overlap + exact/fuzzy gate + accretion records + low-entropy guard; pipeline
reorganize-keeps-hubs + coercion + end-to-end accretion; reprocess service (reset → chronological
replay → recompute → commit, failure counting, single-flight, merge reporting); admin `/reprocess`
router. Full suite **344 → 374**, ruff clean. Real-DB SQL smoke advanced to **29 checks** (+3: the
token-overlap `&&` query, empty-tokens=exact-only, candidate `store_path`) vs local pgvector, and a new
**`scripts/smoke_reprocess.py`** validates the destructive reset SQL in a **rolled-back** txn (11 checks:
TRUNCATE CASCADE empties nodes/chunks/edges/node_profiles + review_queue, captures preserved, rollback
restores dev data) — proving the SQL without wiping the dev DB.

**Independent review (fresh agent, diff vs ADR-038…042 + 08 task 11 + CLAUDE.md 1–11):
APPROVE-WITH-MINORS — no must-fix.** It confirmed all five ADRs implemented correctly (traced the
real paths), `TRUNCATE nodes CASCADE` safe (only chunks/edges/node_profiles reference `nodes`; raw +
`app_settings` survive), the exact-only short-circuit / no-fuzzy-guess, chronological+sequential
replay, confirm-gated single-flight, and the `idea`-content reclassification consistent everywhere.
4 minors; 1 fixed, 3 logged:
- **Fixed:** the review-resolution accretion **write** branch was untested (the task-4 test's
  candidate wasn't indexed, so it returned before writing) → added
  `test_pick_candidate_accretes_new_surface_form` (indexed hub + a new significant surface form →
  asserts the alias is accreted + the hub reindexed). 375 tests.

**Follow-ups (logged, not blocking):**
- **Intra-capture variant split** (reviewer #1): two *different* surface forms of the same *new*
  entity in ONE capture still mint two hubs — intra-capture dedup keys on exact `(norm_name, type)`,
  and a just-minted hub isn't in the DB when the second mention resolves, so token-overlap can't
  retrieve it. Not the documented defect (that was cross-capture, and IS fixed — incl. via
  chronological reprocess); own ADR when addressed (resolve mentions against in-flight
  `new_documents`, or intra-capture token dedup of mints).
- **Reprocess per-capture visibility** (reviewer #3): the reprocess aggregate `agent_runs` summary
  omits per-capture coerced/accreted counts (the per-capture `_Interaction` isn't persisted; a
  `logger.info` records coercions). Add coerced/accreted totals to the reprocess `details` to make
  the heal auditable.
- **Exact-leg whitespace asymmetry** (reviewer #4): `normalize_alias` collapses internal whitespace
  but the SQL exact leg (`lower(title)=$1`) doesn't — a stored form with doubled spaces would miss
  the exact leg (negligible: forms are stripped/single-spaced on write; token leg + Python
  `_is_exact` recover it). Fold-normalize on write or compare on a collapsed expression to remove it.

**Local end-to-end reprocess dry-run (2026-07-14): GREEN — the destructive proof.** Ran against a
throwaway local pgvector + a local Ollama (`nomic-embed-text`) + live `claude-max` organize. Seeded 4
defect-repro captures through the real capture pipeline (chronological: create→drain each), then
`python -m app.cli reprocess-all` actually wiped the DB + store (removed 10 node files, `TRUNCATE
nodes CASCADE`) and replayed all 4 from raw: **4/4 re-ingested, 0 failed, 0 inbox fallback, 10 nodes
rebuilt**. A before/after snapshot (scratch harness: real capture pipeline seed + a DB/store-scan
report) asserted the task-11 fixes hold end-to-end:
- **ADR-040 accretion** — "Horia" (cap1) + "Horia Fenwick" (cap3) resolved to ONE person hub
  (`aliases=[Horia, Horia Fenwick]`); no split, and the accretion survived the wipe+replay.
- **ADR-038** — every store-file canonical edge resolves to a live node (no dangling edges); shared
  hubs (Braindan, Horia — each referenced by multiple memories) preserved across the replay.
- **ADR-041** — `Ștefan`/`Timișoara` folded to `stefan`/`timisoara` in every derived field, while the
  raw captures keep their diacritics (data survival / P10).
- **Reset SQL** — derived state wiped and rebuilt; captures + raw preserved.

**Dry-run surfaced a prod-dormant provider bug (fixed, commit `b0148a4`).** The `claude-max` provider
ran `subprocess.run(text=True)` with no `encoding=`, so on a non-UTF-8 host (Windows dev) the CLI's
UTF-8 output was decoded as cp1252 → organizer diacritics were mojibaked (`Ștefan`→`E tefan`,
`Timișoara`→`TimiETMoara`) BEFORE the ADR-041 fold ever ran. **No effect in prod** (the Docker/Linux
locale is already UTF-8, so text-mode default was UTF-8 there) — pinned `encoding="utf-8"` on both
`subprocess.run` calls so folding is faithful on any host; the raw capture was intact throughout
(never-lose held). Also gitignored the local `graph-store/` a dev run bootstraps in-repo (a missed
follow-up from the task-9 vault→graph-store rename, matching the existing `/data/` ignore). ruff
clean, **375 tests green**.

**Independent review of the fix (fresh agent, diff `b0148a4` vs ADR-041 + CLAUDE.md rules):
APPROVE-WITH-MINORS — no must-fix.** It verified the fix is correct, the prod no-op claim is TRUE
(the Linux container already decoded strict UTF-8), `.gitignore` matches (no inline-comment trap),
and gates are green. It flagged a **pre-existing** rule-7 gap (not a regression): a stray invalid
byte from the CLI would raise `UnicodeDecodeError` (a `ValueError`) that escapes `complete`'s
`except (OSError, SubprocessError)` and crashes instead of degrading to the fallback chain.
**Applied that minor (commit `f8a0e1b`)** — `errors="replace"` on both `subprocess.run` calls (degrades
to U+FFFD, folds away downstream), closing the gap on POSIX too + making prod strictly more robust,
and added the **first `ClaudeMaxProvider` tests** (encoding/errors kwargs pinned; non-zero rc + empty
output → `ProviderUnavailable`). ruff clean, **378 tests green** (+3). **Logged follow-up:** broaden
`ClaudeMaxProvider` coverage (the degrade-path is asserted at the kwargs boundary, not yet an
end-to-end invalid-byte decode).

**Pushed + deployed + prod reprocessed (2026-07-14) — task-11 fixes verified live.** Pushed
`f8a0e1b` (`8ec472a..f8a0e1b`); CI green, all deploy jobs succeeded (no migration shipped — prod was
already at 007). User triggered `POST /admin/reprocess` (confirm→apply). The op **succeeded: 9/9
captures re-ingested (28 nodes, 2 inbox), 0 failed; 13 files removed; push=True** (~86s). Verified
against the prod Supabase DB (read-only, before vs after):
- **ADR-039/040 (over-extraction + split heal):** the two pre-fix `Mădălina Fairfax` person nodes
  (one an over-extracted descriptive phrase) → **one** folded hub `person/madalina-fairfax`; `Horia`
  → one hub with aliases `Horia Fenwick|Horia` (accreted).
- **ADR-041 (folding):** **zero** unfolded diacritics in any derived field (pre-fix `Mădălina`/
  `m-d-lina` slugs gone); the only remaining non-ASCII are en/em dashes (`0x2013`/`0x2014`) in
  title/disambig prose — punctuation, correctly not folded. Raw kept: 3 captures still carry
  diacritics in `raw_text` (P10 data survival held).
- **ADR-038:** **0** dangling edges (src and dst); **0** entity-typed nodes in `inbox/`.
- **Counts:** 9 captures preserved; 17→28 live nodes, 17→28 chunks, 15→21 canonical + 140 derived
  edges; 0 pending review. 2 captures landed in `inbox/` (organizer low-confidence → the
  model-failure fallback, working as designed). Two cosmetic organizer-extraction quirks (not
  task-11 defects): a `Bogda Ionita` hub (missing 'n') with `Bogdan Ionita` accreted as an alias.

**Open:** remaining task-10 Accept criteria (reindex parity, profile-in-search,
vocab-proposal→consolidation, `ENTITY_MATCH_MIN_CONF` tuning) → user archives `PSB-vault`. Docs
updated same change set: 02 §2/§5, 03 §Admin, 04 §1, 08 plan task 11. Code pushed through `f8a0e1b`.

## Task 10/11 — remaining M3 live Accept criteria (2026-07-14, all GREEN)

**Accept session (no code — pure live verification against prod `aegauzpsyybfknddxlbw` via the
Supabase MCP, read-only except the reindex-parity wipe + a reverted synthetic vocab seed).** The
four remaining task-10 criteria all pass; M3 is accepted pending only the `PSB-vault` archive.

- **Reindex parity — EXACT (Rule-1 / ADR-001 store-is-truth durability).** Snapshot-guarded full
  wipe: copied `nodes`/`chunks`/`edges` to `*_bak_accept` tables, `TRUNCATE nodes CASCADE`
  (nodes/chunks/edges/node_profiles → 0; `captures`/`review_queue`/`app_settings`/auth untouched —
  FK check confirmed those four are the only `nodes`-referencing tables, all `ON DELETE CASCADE`),
  then user clicked **Admin → Reindex now** (`POST /admin/reindex`). Rebuild from the `PSB-graph`
  store (head `9f28668`, 28 node files incl. 2 `inbox/`) restored a **byte-identical** index:
  nodes 28, chunks 28, canonical 21 (fp `7b7252d6…`), derived 140 (fp `d054e0e2…`), node-id set fp
  `e31b2785…` — every fingerprint matched the pre-wipe snapshot, and a direct `EXCEPT` set-diff was
  **0/0** in both directions. Even derived similarity edges are deterministic (fresh re-embed of the
  same text). Node ids are stable (read from store frontmatter). Snapshot dropped; prod clean.
- **Profile-in-search — ADR-037 confirmed live.** *Finding first:* `node_profiles` was **empty** on
  prod — the task-11 reprocess `TRUNCATE`d it and neither reprocess nor `/admin/reindex` regenerate
  profiles (profile-refresh is nightly-only, `10 4 * * *`), so search had run since the 2026-07-14
  reprocess with **no profile leg** until the next nightly. Ran the on-demand CLI on the VPS
  (`docker compose exec api uv run python -m app.cli profile-refresh`) → **17 profiles** (all 17
  entity hubs), all 768-dim embeddings + non-empty observation-style text; HNSW index
  `node_profiles_embedding_hnsw` (`vector_cosine_ops`, migration 007) present. **Data-layer proof:**
  ran the exact `/search` two-leg `UNION ALL` (`store.py`) with a memory's embedding as the query
  vector — the `Madalina Fairfax` + `Horia Fenwick` person hubs surfaced **only via the profile leg**
  (scores 0.93 / 0.82, `[involves]…` profile snippets), not their thin chunks. **PWA proof:** live
  search "Madalina" → top result the `Madalina Fairfax` **person hub** (0.67) with the derived profile
  as snippet. → **Follow-up (logged):** reprocess/reindex leave `node_profiles` empty until the
  nightly job — a silent profile-in-search degradation window; consider having `POST /admin/reprocess`
  (and/or reindex) trigger a profile-refresh, or document "run `profile-refresh` after a reprocess."
- **Vocab-proposal → consolidation round-trip — ADR-035 task-7a chain confirmed.** Seeded a
  `vocab-proposal` review item `{vocab:edge_rel, value:reports_to}` (not in the 6 seeded rels); user
  clicked **Approve** in the Review tab. Verified: `app_settings.vocabulary` gained
  `edge_rels:["reports_to"]` (forward-live; was absent), a feed-visible `vocab-consolidation`
  `agent_runs` entry **succeeded** with `details.review_id` linking back, and the item flipped to
  **resolved** with `resolution.run_id` matching the run. Retro-retype correctly marked
  `retro_consolidation: pending` (the apply is the separate on-demand `POST /admin/vocab/consolidate`,
  ADR-036, not the 7a auto-run). The synthetic seed was **reverted fully** (deleted the run + review
  item + the `vocabulary` key) to leave prod vocab/feed reflecting only organic activity.
- **`ENTITY_MATCH_MIN_CONF` — keep 0.8 (user decision), satisfied-as-observed.** Evidence across 9
  captures / 17 hubs: exact matches auto-accrete (Horia, etc.), the one fuzzy case
  (Horia→Horia Fenwick) correctly routed to review at conf < 0.8 and resolved — **zero false
  auto-merges**; the `Bogda Ionita` typo hub is an LLM extraction error (it accreted `Bogdan Ionita`
  as an alias), not a threshold miss. Sample too small to tune; 0.8 behaves conservatively per
  ADR-040 "never guess." **Follow-up (logged):** revisit as captures grow.

**No code changed this session** (verification only; the one synthetic DB seed was reverted), so no
diff and no independent review applies. Prod end state: 28 nodes / 28 chunks / 161 edges (21+140) /
**17 profiles** (net-new, carried forward) / 1 resolved review item / vocab at seed defaults.

**`PSB-vault` archived by the user (2026-07-14) → M3 CLOSED.** The pre-pivot vault GitHub repo is now
read-only (zero manual VPS steps, ADR-031 — the VPS runs off `/srv/graph-store` ← `PSB-graph`; the old
`vault_deploy_key` was already repurposed as `graph_store_deploy_key`). All M3 acceptance criteria pass;
the graph-native stack is live at `braindan.cc`. Code unchanged (through `f8a0e1b`). **Next milestone:
M4 (chat) — a planning session (`/grilling` first per 09).**
