# M8.2 — implementation log (data quality: interiority + temporal correctness)

Per-task build narratives for **M8.2** — what was built, the independent review, and the
verification. The plan, scope, acceptance criteria, and the live task checklist live in
[../08-implementation-plan.md](../08-implementation-plan.md) (§M8.2). This file is append-only
detail; nothing here is a contract — the contract is 00–09 + the ADRs (here,
[ADR-055](../adr/055-interiority-inner-voice-first-class.md) ·
[ADR-056](../adr/056-temporal-correctness-date-tokens.md)).

## Task 1 — Temporal engine (symbolic schema · resolver · tokens · render)

**The pure-logic core of "LLMs classify, code computes"** ([ADR-056](../adr/056-temporal-correctness-date-tokens.md),
CLAUDE.md hard rule 12). First task in the milestone; **no migration, no DB, no LLM, no wiring** —
all downstream consumers (organizer T2, indexer/chat/MCP/capsule T3, web mirror T4) build on this.
New package `server/app/temporal/`. **Committed locally (`d30d6b6` + review fixes `2787701`), not
pushed.**

### What was built

- **`symbolic.py` — the emission contract.** A fail-closed pydantic discriminated union (on `kind`)
  of exactly what the organizer LLM may emit — never a computed date, only a *classification*:
  `ExplicitRef` (whole/partial calendar date, year optional → code snaps), `RelativeRef`
  (`unit` day/week/month/year × signed `offset`), `WeekdayRef` (weekday × last/this/next),
  `MonthRef` (month × year|direction), `SeasonRef` (season × year|year_offset). `extra="forbid"`
  guards against silent drift; validators coerce common variants ("Tuesday"→`tue`, "fall"→`autumn`).
  `parse_symbolic(data)` returns `None` on anything invalid — **never raises** on bad model output.
- **`resolver.py` — the deterministic compute.** `resolve(ref, anchor)` / `resolve_reference(data,
  anchor)` turn a symbolic ref into an absolute `ResolvedTime` against the capture's **stored
  anchor** (never wall-clock — reprocess-determinism, ADR-056 §1): offset arithmetic, weekday walks
  (last/this/next, `or 7` so same-weekday goes back a full week), month/year snapping, yearless
  snap to the most-recent-past occurrence (year-by-year back-walk, 8-year reach covers a century
  leap gap), northern-hemisphere season windows (winter spans the year boundary). Anything it can't
  compute to a real date → `None` (fail-closed, rule 12). Pure stdlib (`datetime`/`calendar` +
  integer month math), **no `dateutil`** — so the T4 web mirror computes byte-identical results.
- **`tokens.py` — the `[[t:START[/END][|label]]]` token + `PartialDate`.** Partial-ISO at honest
  granularity (`2025` / `2025-07` / `2025-07-07` / `2025-07-07T22:00`); parse/serialize, `find_tokens`
  locates every token in a body (malformed → `resolved=None`, surfaced not crashed), and the
  **day-granular `occurred` floor/ceil** the DB stores (`occurred_*` stay `date` — tokens own sub-day,
  ADR-056 §6; a coarse single partial honestly spans its period for the ADR-049 dedup gate, a
  precise day/minute is a point → `occurred_end` None).
- **`render.py` — tokens → text, never shown raw** (ADR-056 §4). `render_relative` = the live phrase
  ("10 days ago", "last month", "summer 2025") computed at *now*, with `render_absolute` as the
  tooltip; `expand_for_index` = stable absolute only (vectors see language, not token noise);
  `expand_for_llm` = absolute + a fresh relative hint ("7 July 2026 (10 days ago)") for the LLM-bound
  rendering contract. Whole-body helpers replace every token. The humanization spec is documented for
  the web mirror; **rounding pinned to round-half-up** (`math.floor(x+0.5)`) so JS `Math.round` agrees
  at `.5` ties.

### Tests, lint, review

- **57 unit tests, zero mocks** (`tests/test_temporal.py`) — schema validation/fail-closed, every
  resolver branch against a fixed **Friday** anchor (2026-07-17), token round-trips + `occurred`
  mapping, and all four render audiences. Both ADR-056 Accept scenarios exercised: "10 days ago" →
  "10 days ago" today / "a year ago" next year (tooltip exact date); "last summer" →
  `[[t:2025-06/2025-08|summer 2025]]`. **Full suite 882 green, ruff + format clean.**
- **Independent review** (fresh agent, ADR-056 + 08 §M8.2 + CLAUDE.md + diff): **CHANGES-REQUIRED**
  → **1 must-fix + 2 minors, all resolved** (`2787701`).
  - **MUST-FIX (resolved):** yearless "29 Feb" against a leap-year anchor fell through to a *future*
    date when the `y-1` candidate was invalid (`stepped or candidate` kept the known-future date).
    Rewrote yearless snapping to the back-walk described above → previous real leap year (2020), or
    `None` when no valid past occurrence is in reach. Regression-tested.
  - **MINOR (resolved):** `round()` half-to-even vs JS `Math.round` half-up would diverge at `.5`
    ties in the not-yet-built web mirror → pinned to round-half-up (`_round`), documented in the spec.
  - **MINOR (resolved):** test-coverage adds — Feb-29 leap boundary, impossible-day→None, round-half-up
    ties, future coarse (month/year) rendering, mixed-granularity range round-trip.
  - The reviewer independently re-verified all resolver math, weekday walks, season windows, token
    round-trips, both Accept scenarios, web-portability, and rule-12 fail-closed discipline as correct.

**Next: M8.2 Task 2** — organizer v6 + migration 016 (anchor injection, symbolic time-reference
emission → server-side resolution into `occurred` + body tokens, `interiority` stamp + inner-voice
extraction, fail-closed validation, prompt-version bump).

## Task 2 — Organizer v6 + migration 016 (anchor · symbolic time → occurred + tokens · interiority)

**Where the temporal engine (T1) and the interiority marker meet the organizer** —
[ADR-056](../adr/056-temporal-correctness-date-tokens.md) §1–§3/§6 (anchor injection, "LLMs
classify, code computes", inline tokens, date-granular `occurred`) +
[ADR-055](../adr/055-interiority-inner-voice-first-class.md) §1/§2 (the marker + inner-voice
extraction). **Committed locally (`afd959f` + review minors `8e85a67`), not pushed.**

### Grill decision resolved this session (inner-voice edge)

ADR-055 §2 requires the extracted `internal` node to be **"edge-linked to the event node it arose
from,"** but the codebase had **no intra-capture content→content edge mechanism** (edges only ever
targeted resolved *entity hubs*) and the ADR **named no rel**. Surfaced to the user as an explicit
fork; **user chose: reuse the seeded `led_to` rel** — the edge sits on the **EVENT** node → the
internal node, and the organizer references the sibling by **local index** (`arose_from`); **no new
vocabulary** (aligns with §2's "existing types" minimalism — a later `arose_from` rel or a
`feeling`/`reflection` type stays a trivial migration since internal nodes are already identifiable).
Recorded here + in [../08-implementation-plan.md](../08-implementation-plan.md) §M8.2 Task 2 (no new
ADR — a mechanical elaboration within ADR-055 §2, not a new architectural axis).

### What was built

- **Organizer prompt v6** (`app/capture/organizer.py`). New `{anchor}` line
  (`render_anchor` — "recorded on Thursday, 2026-07-17 08:40 (Europe/Bucharest)", the capture's
  **stored** time, never wall-clock → reprocess-deterministic). The emitted JSON drops the old
  LLM-computed `occurred` and gains **`time_refs`** (a list of symbolic classifications — the exact
  five `symbolic.py` kinds with a `phrase` + an optional `event` flag), **`interiority`**
  (`internal|external|mixed`), and **`arose_from`** (the local index of the sibling event node an
  `internal` node came from). The prompt spells out that the model **only classifies** each time
  phrase (never a date), keeps the phrase as prose, and marks recurrence as prose (no token).
- **`validate_organizer_output` gained an `anchor` param + a two-pass build.** Pass 1 builds each
  surviving node and resolves its `time_refs` (`_resolve_time_refs` → the T1 `resolve_reference`):
  every resolvable ref's `phrase` is spliced out of the body for its `[[t:…]]` token (overlap-safe
  first-occurrence splice; unresolvable → **no token, phrase stays prose**, rule 12), and the `event`
  ref supplies the **day-granular** `occurred`/`occurred_end` (`_date_iso` drops any `T..:..` — tokens
  own sub-day, §6). `interiority` validates to the three values (`external` default; hubs left
  unstamped). Pass 2 **remaps `arose_from`** from the LLM's raw node index to the surviving node's
  result index (dangling/self → `None`), so a dropped earlier node can't misdirect the edge.
- **Pipeline** (`services/capture_pipeline.py`). `created_local` is computed **before** organize at
  all three call sites (`_process`, `reprocess_capture`, `_replace_notes_via_reorganize`) and threaded
  as the **single anchor** into both the prompt and validation. `_build_documents` stamps
  `interiority`/`occurred_end` on content nodes and draws the **event `led_to` internal** sibling edge
  from `arose_from` (bounds-checked, non-self; `_INNER_VOICE_REL = "led_to"`).
- **Persistence.** `NodeDocument.interiority` (None on hubs) renders a frontmatter `interiority:` line
  (`node_writer`, `ORGANIZER_VERSION` v3→**v4**); the frontmatter parser reads it back
  (`_clean_interiority` → NULL on unknown); `NodeUpsert.interiority` + the `nodes` upsert SQL (`$19`,
  ON CONFLICT) persist it; the indexer passes `meta.interiority`. **Migration 016** adds the nullable
  `nodes.interiority` column (additive, reversible, no backfill — reprocess fills it, Rule-1
  rebuildable).

### Tests, verification, review

- **+18 unit tests** across `test_capture_organizer` (symbolic event/season/non-event/unresolvable/
  phrase-absent/no-refs/anchor-determinism/sub-day-granularity; interiority default+valid; `arose_from`
  remap/dangling/self/reindex-on-drop/non-int; `render_anchor`), `test_node_writer` (interiority +
  occurred-range render→parse round-trip; hub omits it), `test_frontmatter` (valid/invalid/absent
  interiority), and an end-to-end `test_capture_pipeline` inner-voice case (mixed capture → external
  event + internal feeling, `led_to` edge by id, occurred from anchor, body token, anchor injected).
  `test_config_and_migration` head bumped 015→**016**. **Full suite 900 green, ruff + format clean.**
- **Real-PG checks** (the un-fakeable SQL): migration 016 **up/down/up round-trip** on local pg
  (column present/nullable/text → dropped → restored, head=016); real `PgIndexStore.upsert_node`
  **insert + ON CONFLICT** of `interiority` (internal → mixed) round-tripped and cleaned up.
- **Independent review** (fresh agent — ADR-055/056 + §M8.2 + CLAUDE.md + the `afd959f` diff):
  **APPROVE-WITH-MINORS — no must-fix.** It re-traced the full flow (prompt→validate→resolve→build→
  write→parse→upsert) and confirmed all 8 checks (fail-closed, anchor-not-wall-clock across all 3 call
  sites, date-granular occurred, interiority content-only, the `led_to` edge + `arose_from` remap
  incl. index-shift, never-crash, migration additive/reversible, no missed in-scope criteria). Two
  **minors applied** (`8e85a67`): a dedicated sub-day `occurred` test (§6 invariant) + a comment on
  the first-occurrence splice.

**Next: M8.2 Task 3** — consumers (server): indexer token expansion before embedding; the LLM-bound
rendering-contract sweep (MCP `render.py` / chat prompt / capsule source / profile gen / consolidation
— token expansion + per-item temporal metadata header); chat-retrieval interiority boost knob; capsule
internal slice; `occurred-enrichment` nightly step + review kind + resolver reuse; two-tier edit
endpoints (token edit + anchor edit → `reorganize_capture_now`). `depends-on:` T2.

## Task 3 — consumers (server) · PART 1 of 2 (read-side + capsule + boost; edits/enrichment deferred)

Task 3 is large (six sub-parts); by user call (2026-07-17) the **read-side half landed first** and the
two **data-critical write halves** — the `occurred-enrichment` review kind and the two-tier edit
endpoints — were **deferred to a fresh session** for a focused pass (clean handoff over one long
session; the independent review runs at the full Task-3 boundary when E+F land). **Committed locally
(`7f2271e` feat + `edc6e80` tests), not pushed.**

### Done this session (A–D)

- **A · Indexer token expansion before embedding** (ADR-056 §4). `Indexer._index_one` runs the body
  through `temporal.render.expand_body_for_index` **before** `chunk_node`, so `[[t:…]]` tokens become
  stable **absolute** language ("summer 2025") in `chunks.content` — the vectors **and** the FTS
  `tsvector` (generated off `chunks.content`) see prose, not token noise. `content_hash` stays keyed on
  the **untouched** file, so hash-skip still tracks the store, not the expansion.
- **B · LLM-bound rendering-contract sweep** (ADR-056 §4 — every path that puts node/capture content
  before an LLM ships token expansion + a per-item **temporal metadata header** `recorded 7 July 2026
  (10 days ago) · occurred 27 June 2025`). New `temporal.render.temporal_header` + `format_occurred`
  helpers (deterministic, `now`-parameterised — a live render, wall-clock legal per rule 12). Swept:
  **chat** (`render_context` gains the header + `expand_body_for_llm` on the snippet; `SearchHit` now
  carries `occurred_*`/`created_at`/`interiority`), **MCP** (`render_search_results`/`render_node`/
  `render_build_context` gain `now`, the header, and body/profile expansion; `NodePreview`/`NodeRow`
  carry `created_at`/`interiority`), **capsule source** (recent-node lines get the header + the excerpt
  is already index-expanded). **Verified N/A** (no node bodies reach the LLM, dates already absolute):
  **profile-gen** (observations = `[rel] title (as of date)`, ADR-031(c) "never node bodies") and
  **tag/edge consolidation** (tag counts / edge rels only) — recorded as no-change, mirroring T3-of-M4's
  two-endpoint no-change pattern.
- **C · Chat-retrieval interiority boost** (ADR-055 §3a). `RetrievalParams.interiority_boost` (default
  **1.0 = neutral**, so `/search` is untouched) multiplies the fused RRF×recency score for `internal`
  nodes in the `scored` SQL CTE (`CASE WHEN interiority = 'internal' THEN $14 ELSE 1.0`). `SearchService.
  search` + the chat `Retriever` protocol gain the kwarg; chat passes the new `chat_interiority_boost`
  (**1.2**, `Field(ge=1.0)`). Only chat grounding gets the thumb on the scale.
- **D · Capsule internal slice** (ADR-055 §3b). `CapsuleSourceStore.recent_internal` (newest live nodes
  stamped `interiority='internal'`, any type) + a **labeled "inner voice" section** in the distiller
  source (`render_capsule_sources`), counted in `CapsuleOutcome.internal` + provenanced (`kind:internal`).
  `identity_capsule_max_internal` = **10**. `_recent_of_type` generalized to `_recent(type|interiority)`.

**Tests / gates.** +8 unit (temporal header/`format_occurred`; indexer expansion → token-free chunks;
`render_context` header+expansion; interiority-boost plumbed to the retriever; capsule internal
slice counted+provenanced + section render) — **908 green**, ruff + format clean. The changed
**search SQL** (interiority boost + carried `occurred_end`/`created_at`/`interiority` columns) and the
generalized capsule-source SQL were exercised by the **real-PG smoke — 156/156** on local pgvector
(the un-fakeable path; the neutral-boost search checks all pass with the new query). A dedicated
boost/internal-slice smoke check is a follow-up for when the milestone reprocesses (T5).

### Deferred to a fresh session (E–F) — the write-side half

- **E · `occurred-enrichment` review kind** (ADR-056 §7): a nightly step flags undated/coarsely-dated
  content nodes (graph-health already counts them) → a review card asking for the event time in NL →
  the **same symbolic-classification + deterministic resolver** (anchored to the answer's own date) →
  the **mechanical apply** of §5. Needs: a focused NL→symbolic time classifier (reuse the organizer's
  emission + the T1 resolver, routed `conspect`), a new `review_queue` kind wired through
  `review_service`/router/graph-health, and a nightly pipeline step in the roster.
- **F · Two-tier edit endpoints** (ADR-056 §5): **token edit** = mechanical (rewrite the `[[t:…]]`
  token, update `occurred` if it is the event date, re-embed — no LLM, instant) and **anchor edit** =
  background one-capture `reorganize_capture_now` (already exists on `CapturePipeline`). Needs a
  `NodeWriter` token-replace + scalar-`occurred` frontmatter setter (pure + tested, model: `retype_edge`),
  two endpoints, and the 03-api contract pinned (the M8.2 addendum names them but not their shapes).

**Next (fresh session): M8.2 Task 3 · PART 2 — E (occurred-enrichment) then F (two-tier edits), then
the independent review over the whole of Task 3, then record + pause.**

## Task 3 — consumers (server) · PART 2 of 2 (the write-side half: E + F)

The two data-critical write halves, built in a focused session. **Committed locally
(`5c4ccd7` feat + `2911f42` tests + `0417079` review-fix), not pushed.**

### E · `occurred-enrichment` review kind (ADR-056 §7)

- **The nightly flagger** — new `app/services/occurred_enrichment.py`: `OccurredEnrichmentService`
  (DB-only, like graph-health/maybe-digest — its own `agent_runs` row, never raises, rule 7) files
  one `occurred-enrichment` review item per **undated content node** returned by
  `PgOccurredEnrichmentStore.undated_content_nodes` (live, non-`inbox/`, **content** type — not an
  entity hub, `occurred_start IS NULL`, recent-first, **bounded** by `occurred_enrichment_max_per_run`
  = 20). **Idempotent (rule 6):** a `NOT EXISTS` excludes any node that already has an item in an
  **already-handled** status — `pending`/`maybe`/**`discarded`** — so a rerun never double-files and a
  *dismiss sticks* (a resolved node self-excludes via `occurred_start IS NULL`). Payload = `{node_id,
  title, type}` (a node **id**, not names — a reprocess rebuilds nodes, so a stale item is harmless).
  **Scope note (recorded, not silent):** the first cut flags the unambiguous **undated** case
  (`occurred_start IS NULL` — the "graph-health already counts them" signal); "coarsely-dated" is
  genuinely underspecified against the day-granular `occurred_*` columns (a real year-long event is
  indistinguishable from a year-granular guess), so refining it into a separate flag is a logged
  follow-up, not folded in.
- **The NL classifier** — new `app/services/nl_time.py`: `NlTimeClassifier.classify(phrase, anchor)`
  runs one `conspect` call whose prompt asks for a **single symbolic time-ref** (the *exact* schema the
  organizer emits, minus `event`; rule 12 — the model never emits a date) then the **T1 resolver**
  computes the absolute date against the anchor. **Fail-closed:** a down provider, a `{"kind":"none"}`,
  or an unresolvable classification → `None`. Reuses the organizer's `render_anchor` + `parse_organizer_json`.
- **The resolution** — `ReviewService` gains a `time_classifier` dep + an `answer` param;
  `_resolve_occurred_enrichment` runs the answer through the classifier **anchored to now (local)** —
  a live user correction, not a replayed path, so wall-clock is legal (rule 12) — then applies the
  **mechanical tier of §5** (`NodeWriter.set_occurred` day-granular partial-ISO + re-index + commit).
  `maybe` parks; `skip`/`unknown`/`dismiss` → discarded (node stays undated); an uninterpretable
  answer → `400` (item stays decidable, rephrase); a node reprocessed away since filing → discarded
  (stale). The `answer` threads through the router + `resolve_batch` (a batch `skip` clears many).

### F · Two-tier date edits (ADR-056 §5)

- **NodeWriter pure helpers + methods** (modelled on `retype_edge`): `replace_body_token` (rewrite the
  first `[[t:…]]` in the **body** only, no frontmatter), `set_occurred_frontmatter` (upsert/clear the
  `occurred`/`occurred_end` scalars in contract key order), plus the atomic methods `set_occurred`
  (E's apply) and `edit_time_token` (F's mechanical edit — body rewrite + optional occurred set in one
  write). `ResolvedTime` gains `start_date_iso`/`end_date_iso` (day-granular partial-ISO for
  frontmatter, granularity preserved); the organizer's private `_date_iso` was retired onto them (boy-scout).
- **`NodeTimeEditService`** (`PUT /nodes/{id}/date-token`) — reads the node via the search service
  (store_path + the event `occurred` span), decides **is-this-the-event-date** by comparing the
  day-granular occurred span, rewrites the token, moves `occurred` only when it *is* the event date,
  re-embeds (rule 1 — store written then re-indexed, DB never touched directly). `404` unknown/merged;
  `400` bad token/date or token-not-in-body.
- **Anchor edit** (`PUT /captures/{id}/anchor`) — `CapturePipeline.edit_anchor` overwrites
  `captures.created_at` (`CaptureStore.set_created_at`) then spawns the existing one-capture reorganize
  (`_reorganize`) in the background (202). Reprocess-**safe** — the anchor is capture data the replay reads.
- **Wiring** — config `occurred_enrichment_max_per_run` + a nightly `occurred-enrichment` step (read-mostly
  tail, before graph-health); scheduler `_step_funcs` + main.py build + roster auto-pickup; the CLI
  `run_pipeline` now also wires **graph-health + occurred-enrichment** so `pipeline nightly` faithfully
  runs the whole roster its docstring claims (a latent gap for graph-health, fixed in passing). New
  models `CaptureAnchorEditRequest`/`NodeDateTokenEditRequest`/`NodeDateTokenEditResponse` + the `answer`
  field; `get_node_time_edit_service` dep.

### Tests / gates

+~55 unit (temporal iso helpers; node_writer token/occurred pure + atomic; NL classifier happy/
fail-closed/down; occurred-enrichment flagger filing/bounds/dismiss-exclusion; occurred-enrichment
resolution happy/maybe/skip/empty/unclassifiable/stale; node-time-edit event/non-event/range/not-found/
bad-date; router smoke for both endpoints + the review `answer` passthrough; pipeline roster order).
**949 unit green**, ruff + format clean. The `PgOccurredEnrichmentStore` SQL is un-fakeable and wants a
real-PG smoke check — deferred to the T5 reprocess (mirrors PART 1's boost/internal-slice smoke deferral).

### Independent review (over the WHOLE of Task 3) — CHANGES-REQUIRED → resolved

A fresh reviewer read ADR-055/056 + 08 §M8.2 + 03-api + CLAUDE.md against the `8e85a67..HEAD` diff:
- **MUST-FIX 1 (fixed, `0417079`):** the token edit never moved `occurred` for a **day-precise** event
  date — the DB `occurred_end` (which the indexer sets `== occurred_start` for a day) never equalled
  `ResolvedTime.occurred_end()` (`None` for a precise point), so `is_event` was always false for the
  common case. Fall back to `occurred_start` to compare like-for-like; the test fake was corrected to
  the realistic `occurred_end` `SearchService.get_node` actually returns (the green test had masked it).
- **MUST-FIX 2 (fixed, `0417079`):** a **dismissed** (`discarded`) occurred-enrichment item was re-filed
  every night — the idempotency guard only excluded `pending`/`maybe`. Now excludes `discarded` too, so
  a dismiss sticks (regression test added).
- **Finding 3 (verified false positive):** the capsule excerpt is **not** rendered raw — it is sourced
  from `chunks.content` (`identity/store.py`), which the indexer already expands to absolute before
  chunking (PART 1-A), so no tokens remain. No change.
- **Minors (logged, not fixed):** (a) a token/enrichment **derived-node date edit is reverted by a later
  `reprocess-all`** (re-derived from raw) — an accepted ADR-042-class caveat (the anchor edit is
  reprocess-safe); surfaced to the user rather than silently accepted. (b) an `internal` memory can appear
  in both the capsule "inner voice" and "recent memories" source sections (harmless double-provenance).
  (c) the occurred-enrichment store SQL wants the T5 real-PG smoke.

**Next: M8.2 Task 3.5 (a replan seam — see below), then Task 4 (web).**

## Task 3.5 — expose `interiority` on the read API (a replan seam)

**Committed locally (`44fb4e1`), not pushed.**

Starting Task 4 (web) surfaced a **plan gap**: T3's consumer sweep wired `interiority` into the
LLM-bound renders, the chat boost, and the capsule slice, but **never exposed the field on the two
web read endpoints** — so the web could not render the ADR-055 §3c inner-voice marker on
`NodePreview` / the Map. Rather than fold server work into a "web" task (ADR-006 — prior web tasks
touched no server code), the user chose to **stop and replan the seam**: it was grilled
decision-by-decision (recorded in [09](../09-session-protocol.md)-style) and split out as its own
**server Task 3.5 before the web task** (numbering stays 3.5 / 4 / 5). **No new ADR** — this is
contract wiring that delivers the already-Accepted ADR-055 §3c; recorded in
[08 §M8.2](../08-implementation-plan.md) + the [03-api](../03-api.md) M8.2 addendum.

### What was built (no migration — the `nodes.interiority` column exists from migration 016)

- **`GET /nodes/{id}`** — a 1-line pass-through: `search/service.py`'s `NodePreview` already carried
  `interiority`, so the router now threads `preview.interiority` → `NodeDetailResponse.interiority`
  (`internal`|`external`|`mixed`|null). No SQL change.
- **`GET /nodes/{id}/neighbors`** — the graph-store SQL now selects `n.interiority` in **all three**
  queries (`neighbors`, `neighbor_zones` window, and the light `center_header`), carried on the
  `NeighborEdge` + `NeighborHeader` dataclasses and mapped by the router into **`MapNeighborItem`**
  (every neighbor — both the grouped page and the `?rel=` "show more" page, which share `_map_neighbor`)
  **and** **`NeighborCenter`** (so the focal node is markable). The two dataclass fields are **trailing
  + defaulted** so every existing constructor (MCP `render`, the unit-test fakes) is unaffected.
- **Deliberately unchanged:** `SearchResultItem` — the search cards stay within ADR-055 §3c's
  "Map/`NodePreview`" wording (recorded, not silent).
- **Marker semantics pinned for T4:** `internal` = full marker, `mixed` = subtle variant,
  `external`/`null` = none.

### Tests / gates

- **Unit (+2):** `test_get_node_exposes_interiority` (pass-through of `internal` **and** a null-hub),
  `test_neighbors_expose_interiority_on_center_and_neighbors` (center = `mixed`, one neighbor =
  `internal`, an unstamped neighbor = `null`). **951 unit green**, ruff + format clean.
- **Real-PG smoke — not deferred:** extended the existing M5-T1 `check_neighbor_zones` (the un-fakeable
  window/keyset SQL) — stamp `C = internal` / `place = mixed`, then assert the `mixed` neighbor and the
  `internal` center flow through and an unstamped neighbor stays `NULL`. **157/157** on local pgvector.
- **Independent review (`/code-review high`) — no findings.** The 8 finder angles surfaced nothing:
  a consistent pass-through threaded store→dataclass→router→pydantic. Cross-checks confirmed no
  exact-equality response assertion breaks (the only one is the `center=None` unknown-node case) and
  the MCP `render_traverse`/`_edge_line` path formats only `dir/rel/title/type/node_id` — it does not
  print `interiority`, so MCP output is unchanged.

**Next: M8.2 Task 4 (web) — token-aware date rendering (live phrase + tooltip, never raw) + tap-to-edit
(date/range picker over `PUT /nodes/{id}/date-token`); anchor-edit affordance (`PUT /captures/{id}/anchor`);
interiority visual marker (Map/`NodePreview` — `internal` full / `mixed` subtle, consuming the T3.5 field);
`occurred-enrichment` review card (NL input). Pure web (ADR-006). `depends-on:` T3.5.**

## Task 4 — web (token rendering + tap-to-edit · anchor edit · interiority marker · occurred-enrichment card)

**Committed locally, not pushed.** Pure web (ADR-006 — **no server code**; consumes the T3.5-exposed
fields + the ADR-056 §5 edit endpoints already landed in Task 3).

### What was built

- **Web temporal mirror — `ui/dateToken.ts`** (new, pure logic, stdlib-only): a **byte-for-byte port**
  of the server's `app/temporal/tokens.py` + `render.py` — partial-ISO parse/serialize with honest
  granularity, the **round-half-up** humanizer (`Math.floor(x + 0.5)`, pinned to the server's `_round`),
  calendar arithmetic via UTC-midnight day counts (no DST drift). Parses `[[t:START[/END][|label]]]`
  and renders the live relative phrase (recomputed against *now*) + the absolute form. Kept
  import-free so it runs standalone.
- **Token rendering + tap-to-edit — `ui/TokenizedBody.tsx`** (new): splits a node body on its
  `[[t:…]]` tokens and renders each as a live phrase via the shared **`ui/HoverTip.tsx`** tooltip
  primitive (extracted from `<TimeAgo>` so the tap/hover portal-positioning code is shared, not
  cloned — `<TimeAgo>` refactored to a thin wrapper, behaviour unchanged; **rule 10**). Tokens are
  **never shown raw** (a malformed token degrades to its label / inner text). Where the body is
  editable (NodePreview passes `nodeId`), tapping a token opens a **date/range picker** (year/month/
  day + optional time + optional range end + label) that `PUT`s `/nodes/{id}/date-token` (client-side
  fail-closed validation via the mirror before send; a server **400** — bad/absent token, uninterpretable
  date — surfaces inline). Wired into **`ui/NodePreview.tsx`** (Search/Chat/Map-drawer all inherit it),
  which also gained a read-only **occurred "when" line** (live phrase + exact-date tooltip).
- **Anchor-edit affordance — `activity/FeedView.tsx`** capture detail: a "Recorded <when> · Edit time"
  row opening a `datetime-local` editor that `PUT`s `/captures/{id}/anchor` (→ background one-capture
  reorganize) and shows a "re-resolving dates in the background…" note; `useEditCaptureAnchor` invalidates
  the list + detail so the re-cycle renders live.
- **Interiority marker (ADR-055 §3c)** — new `ui/interiority.ts` (semantics: `internal` = full /
  `mixed` = subtle / `external`|null = none) + **`ui/InteriorityBadge.tsx`** (an "inner voice" pill,
  accent-2, shown on NodePreview) and, on the **Map**, a subtle accent-2 **ring** around the mark
  (solid for `internal`, thin-dashed for `mixed`) threaded through `graphModel` (`MapNode.interiority`
  from center + neighbors) + `MapCanvas` (draw + hover-label hint). `SearchResultItem` untouched (§3c
  scope). Wire types gained `interiority` on `NodeDetailResponse` / `MapNeighborItem` / `NeighborCenter`.
- **`occurred-enrichment` review card — `review/ReviewScreen.tsx`**: a new card for the `occurred-
  enrichment` kind (payload `{node_id, title, type}`; the node is a clickable `NodeChip`) with a
  **natural-language date input** → `POST /review/{id}` `{answer}`; "Maybe later" → `{answer:"maybe"}`,
  "Not dated" → `{answer:"skip"}`. A **400** ("couldn't interpret — rephrase", ADR-056 §7) is surfaced
  verbatim so the user rephrases (never a guessed date).

### Verification / gate

- **Gate green:** `tsc --noEmit` clean, `eslint --max-warnings 0` clean, `vite build` green.
- **Temporal mirror executed** (compiled `dateToken.ts` and ran it): both **ADR-056 Accept scenarios
  reproduce exactly** — "10 days ago" today → `"10 days ago"`, next year → `"a year ago"`; "last summer"
  (`[[t:2025-06/2025-08|summer 2025]]`) → `"summer 2025"`; plus round-half-up boundaries (100 days →
  `"3 months ago"`), last-year/last-month/today — all matching the server render spec.
- **Independent review (`/code-review high`, 8-angle):** 3 findings — **1 correctness must-fix
  fixed** (the malformed-token degrade sliced `[[t:` as 3 chars → a stray leading `:`; corrected to
  `slice(4, -2)`), **1 simplification applied** (dropped a duplicated partial-ISO regex — the editor
  preview reuses `parsePartial`), **1 cosmetic left** (the read-only occurred line renders a coarse
  year/month node as a full day-range — honest, and the body token carries the nicer labelled form).
- **Not done this session:** the standard real-browser mock walkthrough (no browser tooling available
  in this session) — the live interactive drive lands at **Task 5** (deploy + prod reprocess + live
  Accept), which exercises these surfaces against real data.

**Next: M8.2 Task 5 — deploy the M8.2 range (migration 016), run the prod `reprocess-all-from-raw`
(backfills interiority + tokens; standing-merge caveat reported), verify the Accept block live →
independent review → M8.2 CLOSED.**
